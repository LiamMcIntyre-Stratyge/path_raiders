---
phase: 11-accounts-economy
plan: 05
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - src/types/index.ts
  - src/lib/gameState.ts
  - src/scenes/GameScene.ts
  - src/scenes/LobbyScene.ts
  - src/scenes/AuthScene.ts
  - src/scenes/ProfileScene.ts
autonomous: false
requirements: [ACCT-01, ACCT-02, ACCT-03, ECON-02, ECON-03]

must_haves:
  truths:
    - "gameState + GameStateType carry opponentId (and walletBalance), hydrated from rooms.host_id/guest_id at LobbyScene join (ACCT-01, RESEARCH finding #8)"
    - "GameScene retires client recordResult/win-milestone unlock and instead calls reportMatchResult after game_over for non-practice rooms (ECON-02, D-11, P10 D-13 handoff)"
    - "username is esc()-escaped before innerHTML at GameScene:882 and LobbyScene:100 (ACCT-02/D-14, stored-XSS closed)"
    - "AuthScene calls provision_account after upsertProfile on BOTH signup paths so new accounts get wallet+welcome grant (ACCT-01)"
    - "ProfileScene binds getProfileFull → W/L + balance + owned units + rank placeholder (ACCT-03, D-13)"
    - "spend-to-unlock UI path invokes spendUnlock for the three non-starter units (ECON-03)"
  artifacts:
    - path: "src/lib/gameState.ts"
      provides: "opponentId + walletBalance fields"
      contains: "opponentId"
    - path: "src/scenes/GameScene.ts"
      provides: "submitMatchReport (replaces recordResult); esc on username"
    - path: "src/scenes/AuthScene.ts"
      provides: "provision_account RPC on both signup paths"
    - path: "src/scenes/ProfileScene.ts"
      provides: "profile screen wired to getProfileFull + spendUnlock"
  key_links:
    - from: "src/scenes/GameScene.ts"
      to: "report_match_result (via settlement.ts)"
      via: "submitMatchReport after game_over"
      pattern: "reportMatchResult"
    - from: "src/scenes/LobbyScene.ts"
      to: "gameState.opponentId"
      via: "rooms.host_id/guest_id at join"
      pattern: "opponentId"
    - from: "src/scenes/AuthScene.ts"
      to: "provision_account RPC"
      via: "supabase.rpc after upsertProfile"
      pattern: "provision_account"
    - from: "src/scenes/ProfileScene.ts"
      to: "getProfileFull"
      via: "profile.ts aggregate"
      pattern: "getProfileFull"
---

<objective>
Wire the scenes to the new server-authoritative seam: retire the client-side
`recordResult`/win-milestone unlock (the explicit Phase 10 D-13 handoff), submit a match
report after `game_over`, escape `username` at both XSS sites, call `provision_account` on
signup, hydrate `opponentId`, and bind the profile screen + spend-to-unlock UI to the new
API clients.

Purpose: This completes the authority move — the client no longer writes wins/unlocks; it
submits a claim and reads server truth. Closes the stored-XSS pitfall (D-14) and delivers the
ACCT-02/03 profile surface (integrating the user's provided design).

Output: edited scenes + a wired ProfileScene.

NOTE (P10 dependency): Phase 11 is planned ahead of Phase 10. The match-report submission
consumes Phase 10's sim `game_over`/match-outcome event (P10 D-04) and the slimmed read-through
`gameState` (P10 D-12/D-14). If `gameState.opponentId` / `gameState.walletBalance` are NOT
already present when this executes (RESEARCH finding #8, A1/A5), THIS plan adds them — Task 1 is
defensive and owns those fields regardless of whether Phase 10 added them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-accounts-economy/11-PATTERNS.md
@.planning/phases/11-accounts-economy/11-RESEARCH.md
@.planning/phases/10-services-simulation-refactor/10-CONTEXT.md

<interfaces>
<!-- New API clients (Plan 03) the scenes consume: -->
src/lib/api/settlement.ts:  reportMatchResult(matchId, claimedWinnerId) → {status, error}
src/lib/api/inventory.ts:   getOwnedUnits(userId) → string[];  spendUnlock(unitId) → {ok, reason?, newBalance?, unitId?, error}
src/lib/api/profile.ts:     getProfileFull(userId) → FullProfile|null  (username, wins, losses, balance, ownedUnitIds, rankPlaceholder)
src/lib/api/wallet.ts:      getBalance(userId) → number|null
src/lib/escapeHtml.ts:      esc(s) → string

<!-- Confirmed edit sites (PATTERNS Key Confirmed Facts): -->
account.ts recordMatchResult IMPORT removed; GameScene call site at :601 inside existing !practice guard
GameScene recordResult method :607-617 (REMOVE); showUnlockNotification :619-641 (REMOVE); username :882 (esc); ${username} :1002 (safe after)
LobbyScene username :100 (esc); ${username} :133 (safe after)
AuthScene upsertProfile calls at :624 (signup) and :862 (onboard) — add provision_account after EACH
gameState.ts + types/index.ts GameStateType — add opponentId after `role`
UnitData ids: 'assault_bot','thorn_beast','elementalist' (starter:false)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: opponentId/walletBalance state + LobbyScene hydration + XSS escape</name>
  <files>src/types/index.ts, src/lib/gameState.ts, src/scenes/LobbyScene.ts</files>
  <read_first>
    - src/lib/gameState.ts (READ IN FULL — the default object; add fields after `role`)
    - src/types/index.ts (GameStateType, lines ~18-35 — add the same fields)
    - src/scenes/LobbyScene.ts (READ the room create/join paths where roomId+role are set, and lines 100/133 username sites)
    - .planning/phases/11-accounts-economy/11-PATTERNS.md §"gameState.ts" (lines 347-382), §"types/index.ts" (lines 385-393), §"LobbyScene.ts — edit" (lines 506-535: esc + opponentId hydration)
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"Open Questions" 1-2 (opponentId hydration + UUID validation) and finding #8
    - .planning/phases/10-services-simulation-refactor/10-CONTEXT.md (D-12/D-14 — check whether the slimmed gameState already declares opponentId/walletBalance; if Phase 10 is unexecuted, add them here)
  </read_first>
  <action>
    Add `opponentId: string | null` and `walletBalance: number` to GameStateType in src/types/index.ts (after
    `role`) and to the default object in src/lib/gameState.ts (opponentId:null, walletBalance:0). If Phase 10 already
    added either field, do NOT duplicate — reconcile to a single declaration.
    In LobbyScene.ts: import `{ esc }` from '../lib/escapeHtml'; wrap line 100
    `const username = esc(gameState.username ?? 'COMMANDER')` (line 133 `${username}` becomes safe). Hydrate opponentId
    at the room flow: when a guest joins, set `gameState.opponentId = room.host_id`; when the host sees the guest join
    (realtime room update), set `gameState.opponentId = updatedRoom.guest_id ?? null`. Validate the value looks like a
    UUID before trusting it downstream (Open Question 2 — skip settlement if not a UUID).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "opponentId" src/types/index.ts src/lib/gameState.ts` returns ≥1 each (single declaration, no duplicate if P10 added it)
    - `grep -c "esc(gameState.username" src/scenes/LobbyScene.ts` returns 1
    - LobbyScene sets gameState.opponentId from a rooms field on both join and guest-arrival paths (`grep -c "gameState.opponentId" src/scenes/LobbyScene.ts` returns ≥2)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>opponentId/walletBalance in state, hydrated at join; LobbyScene username escaped.</done>
</task>

<task type="auto">
  <name>Task 2: GameScene — retire recordResult, submit match report, escape username</name>
  <files>src/scenes/GameScene.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (READ the import line 4, recordResult method :607-617, showUnlockNotification :619-641, call site :601, username :882/:1002 — confirm exact current line numbers before editing)
    - .planning/phases/11-accounts-economy/11-PATTERNS.md §"GameScene.ts — edit" (lines 395-468: import swap, removals, call-site replace, submitMatchReport body, esc at 882)
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"GameScene match result submission" (lines 752-781, incl. opponentId-UUID guard) and §"Pitfall 8" (confirm no other importer of recordMatchResult)
    - .planning/phases/10-services-simulation-refactor/10-CONTEXT.md (D-04 game_over event — the winner identity source; D-13 the recordResult retirement handoff)
  </read_first>
  <action>
    Edit src/scenes/GameScene.ts: remove `import { recordMatchResult } from '../lib/api/account'`; add imports of
    `{ reportMatchResult }` from '../lib/api/settlement', `{ getBalance }` from '../lib/api/wallet', `{ esc }` from
    '../lib/escapeHtml'. DELETE the `recordResult` method (:607-617) and `showUnlockNotification` method (:619-641)
    entirely (win-milestone unlock removed — D-11). Replace the call site at :601 (inside the existing
    `if (!gameState.roomId?.startsWith('practice-'))` guard) with `void this.submitMatchReport(playerWon, winner)`.
    Add a `submitMatchReport(playerWon: boolean, winner: 'host'|'guest'|'tie')` method: return early if no
    userId/roomId or winner==='tie'; compute winnerId = (winner===gameState.role) ? gameState.userId :
    (gameState.opponentId ?? gameState.userId); skip if opponentId is not a valid UUID; `await reportMatchResult(
    gameState.roomId, winnerId)`; then refresh `gameState.walletBalance = await getBalance(gameState.userId)`.
    Wrap username at :882 with `esc(...)` (line 1002 `${username}` then safe). The match winner identity comes from
    Phase 10's game_over event (D-04); if P10 emits only role labels, the role→UUID mapping above derives it.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recordMatchResult\|recordResult\|showUnlockNotification" src/scenes/GameScene.ts` returns 0
    - `grep -c "reportMatchResult\|submitMatchReport" src/scenes/GameScene.ts` returns ≥2
    - `grep -c "esc(gameState.username" src/scenes/GameScene.ts` returns 1
    - `npx tsc --noEmit` passes (account.ts no longer exports recordMatchResult; this removes the last importer — Pitfall 8 resolved)
  </acceptance_criteria>
  <done>Client unlock authority retired; match report submitted server-side; username escaped.</done>
</task>

<task type="auto">
  <name>Task 3: AuthScene provision_account + ProfileScene wiring (getProfileFull + spendUnlock)</name>
  <files>src/scenes/AuthScene.ts, src/scenes/ProfileScene.ts</files>
  <read_first>
    - src/scenes/AuthScene.ts (READ the signup block :615-636 and the onboard block :862-874 — both call upsertProfile; add provision_account after EACH success)
    - .planning/phases/11-accounts-economy/11-PATTERNS.md §"AuthScene.ts — edit" (lines 472-503: where to insert the provision_account RPC), §"src/lib/api/profile.ts" (getProfileFull/FullProfile shape ProfileScene consumes)
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"Open Questions" 3 (ProfileScene new vs LobbyScene-integrated — stub it, user integrates their design), §"Profile screen API client"
    - an existing scene (e.g. src/scenes/LobbyScene.ts) for the Phaser scene class skeleton/registration convention to mirror for a new ProfileScene
  </read_first>
  <action>
    AuthScene.ts: after the `upsertProfile` success check on BOTH paths (signup :624 area and onboard :862 area),
    insert `await supabase.rpc('provision_account', { p_user_id: userId })` (non-fatal — if it fails the deploy-time
    backfill still provisions). This guarantees a wallet + welcome grant for every new/onboarded account (ACCT-01, D-02).
    ProfileScene.ts: create (or wire, if a design stub exists) a Phaser scene that on entry calls
    `getProfileFull(gameState.userId)` and binds username, wins/losses, balance, ownedUnitIds, and rankPlaceholder
    ('UNRANKED') to the provided design (ACCT-02/03, D-13 — profile doubles as roster). Add the spend-to-unlock action:
    for each of the three non-starter units (assault_bot/thorn_beast/elementalist) NOT in ownedUnitIds, an unlock control
    that calls `spendUnlock(unitId)`; on `{ok:true}` refresh the owned list + balance, on
    `{ok:false, reason:'insufficient_funds'}` surface a non-blocking message (ECON-03). Display unit cost as a
    cosmetic label only (server is authoritative — never trust the UI value for deduction, D-07). Register the scene in
    the Phaser config alongside the other scenes. The visual design is user-owned (CONTEXT) — this wires data/behavior to it.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "provision_account" src/scenes/AuthScene.ts` returns 2 (signup + onboard paths)
    - `grep -c "getProfileFull" src/scenes/ProfileScene.ts` returns ≥1 and `grep -c "spendUnlock" src/scenes/ProfileScene.ts` returns ≥1
    - ProfileScene references the rank placeholder and the three non-starter unit ids
    - `npx tsc --noEmit` passes; ProfileScene is registered in the Phaser scene list
  </acceptance_criteria>
  <done>New accounts auto-provisioned; profile screen bound to server truth with spend-to-unlock wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify profile screen + earn→spend loop in-app</name>
  <what-built>
    The full client wiring is live: signup provisions a wallet + 100 welcome grant; the profile screen shows
    W/L + balance + owned units + 'UNRANKED'; the three non-starter units can be unlocked via spendUnlock; match
    end submits a report instead of writing wins client-side; username is escaped. UI design is the user's.
  </what-built>
  <how-to-verify>
    1. `npm run dev`; create a NEW account. Open the profile screen — confirm balance shows 100 (welcome grant),
       W/L shows 0/0, rank shows UNRANKED, owned units = the 3 starters only.
    2. Unlock one non-starter unit — confirm balance drops to 0 and the unit appears as owned. Try unlocking a second —
       confirm an insufficient-funds message (no balance change, no unlock).
    3. Play a non-practice match to completion against a second account; both report. Confirm the winner's balance
       rises by 50 and the loser's by 15 on the profile after the match (D-01 split). A practice match grants nothing.
    4. Set a username containing `<img src=x onerror=alert(1)>` at signup; confirm it renders as literal text in the
       lobby + in-game HUD (no alert) — XSS closed (D-14).
    5. Confirm the earn→spend loop feels fast (welcome ≈ 1 unit, further units ~3–5 wins) — D-02/D-03 pacing.
  </how-to-verify>
  <acceptance_criteria>
    - New account shows balance 100 / 0-0 / UNRANKED / 3 starters on the profile
    - One unlock deducts to 0 and grants the unit; a second unlock is refused (insufficient funds)
    - Post-match: winner +50, loser +15; practice match grants nothing
    - XSS username renders as literal text (no script execution)
  </acceptance_criteria>
  <resume-signal>Type "verified" if the profile + earn→spend + XSS behaviors are correct, or describe what is off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sim game_over event → client report | Client derives a winner claim from the sim outcome and submits it; server settles only on agreement |
| user-set username → DOM innerHTML | Must be escaped before interpolation (stored-XSS surface) |
| client unlock UI → spend_unlock RPC | UI cost labels are cosmetic; deduction is server-authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | XSS (Tampering) | username at GameScene:882, LobbyScene:100 | mitigate | esc() applied before innerHTML interpolation (D-14, HIGH); checkpoint verifies a live payload renders as text |
| T-11-15 | Tampering | client-trusted reward via the old recordResult path | mitigate | recordResult/win-milestone unlock deleted; GameScene submits only a winner claim to report_match_result (ECON-02, D-11) |
| T-11-16 | Tampering | forged winner claim (single client) | accept | settlement requires BOTH reports to agree (D-06); single forged claim stays pending; mutual collusion is the accepted interim risk (D-05) hardened in P14 |
| T-11-17 | Tampering | UI cost label trusted for deduction | mitigate | spend_unlock derives cost server-side; UI value is cosmetic (D-07) |
| T-11-18 | Spoofing | stale/invalid opponentId → wrong settlement | mitigate | UUID-validate opponentId before calling reportMatchResult; skip settlement if invalid (Open Question 2) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (no dangling recordMatchResult import).
- Checkpoint: profile balance/W/L/units/rank correct; earn→spend loop and XSS escape verified live.
</verification>

<success_criteria>
- Client recordResult/win-milestone unlock retired; match report submitted to the server (ECON-02, D-11, P10 D-13 handoff).
- username escaped at both sites (ACCT-02/D-14).
- Profile screen bound to server truth; spend-to-unlock wired for the 3 non-starters (ACCT-03/ECON-03, D-13).
- New/onboarded accounts auto-provisioned with wallet + welcome grant (ACCT-01).
</success_criteria>

<output>
Create `.planning/phases/11-accounts-economy/11-05-SUMMARY.md` when done.
</output>
