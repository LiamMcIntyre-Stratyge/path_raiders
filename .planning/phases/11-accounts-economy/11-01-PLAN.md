---
phase: 11-accounts-economy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/escapeHtml.ts
  - test/unit/escape.test.ts
  - test/unit/economy.test.ts
  - test/rls/inventory-rls.test.ts
  - test/rls/settlement-idempotency.test.ts
  - test/rls/migration.test.ts
autonomous: true
requirements: [ACCT-02]
nyquist_role: wave-0

must_haves:
  truths:
    - "esc() escapes <, >, &, \", ' so a username XSS payload cannot reach innerHTML unescaped (D-14, ACCT-02)"
    - "Economy constants (WIN_REWARD=50, LOSS_REWARD=15, WELCOME_GRANT=100, unit cost=100) are asserted positive and internally consistent with D-02/D-03 (D-04)"
    - "RLS/integration test files exist and are RED (their RPCs/columns do not exist yet) so Wave 1+ has a concrete target"
  artifacts:
    - path: "src/lib/escapeHtml.ts"
      provides: "esc() HTML-escape helper (5 replacements)"
      exports: ["esc"]
    - path: "test/unit/escape.test.ts"
      provides: "esc() unit coverage incl. XSS payload"
    - path: "test/unit/economy.test.ts"
      provides: "economy constant assertions (D-02/D-03/D-04)"
    - path: "test/rls/inventory-rls.test.ts"
      provides: "Wave 0 RED scaffold: forged unlock, spend_unlock, concurrent spend"
    - path: "test/rls/settlement-idempotency.test.ts"
      provides: "Wave 0 RED scaffold: idempotency, mismatch-void, lone-report, concurrent second-reporter"
    - path: "test/rls/migration.test.ts"
      provides: "Wave 0 RED scaffold: backfill, migration idempotency, existing-player"
  key_links:
    - from: "test/rls/inventory-rls.test.ts"
      to: "spend_unlock RPC + inventory table (Plan 02)"
      via: "user.rpc('spend_unlock', ...) / from('inventory')"
      pattern: "spend_unlock|from\\('inventory'\\)"
    - from: "test/rls/settlement-idempotency.test.ts"
      to: "report_match_result RPC + match_settlements (Plan 02)"
      via: "user.rpc('report_match_result', ...)"
      pattern: "report_match_result"
    - from: "test/rls/migration.test.ts"
      to: "provision_account RPC + backfill (Plan 02)"
      via: "rpc('provision_account', ...)"
      pattern: "provision_account"
---

<objective>
Lay the Nyquist Wave 0 foundation for Phase 11: the `esc()` XSS helper plus all
test files the economy/migration/RLS code in later waves must satisfy. The pure-unit
tests (`escape.test.ts`, `economy.test.ts`) go GREEN in this plan; the three RLS
integration test files are written RED (they reference RPCs/columns that Plan 02
creates), giving later waves a concrete, executable target.

Purpose: Per VALIDATION.md, every economy/migration behavior has an automated test.
Scaffolding the tests first (TDD-at-wave-0) prevents "looks done but isn't" gaps on
idempotency, concurrent-spend, mismatch-void, and existing-player migration.

Output: `src/lib/escapeHtml.ts` + 5 test files (2 GREEN unit, 3 RED rls).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-accounts-economy/11-VALIDATION.md
@.planning/phases/11-accounts-economy/11-PATTERNS.md
@.planning/phases/11-accounts-economy/11-RESEARCH.md

<interfaces>
<!-- esc() signature later waves import; mirror exactly. -->
src/lib/escapeHtml.ts:  export function esc(s: string): string

<!-- Economy constants (server-authoritative in SQL; mirrored here display-only). D-04. -->
WIN_REWARD=50  LOSS_REWARD=15  WELCOME_GRANT=100  unit cost (assault_bot|thorn_beast|elementalist)=100 each

<!-- RLS test harness env (CI only, never in src/): -->
process.env.SUPABASE_URL  SUPABASE_ANON_KEY  SUPABASE_SERVICE_ROLE_KEY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: escapeHtml helper + escape.test.ts + economy.test.ts (GREEN)</name>
  <files>src/lib/escapeHtml.ts, test/unit/escape.test.ts, test/unit/economy.test.ts</files>
  <read_first>
    - test/unit/pathfinder.test.ts (the ONLY existing unit test — copy its `import { describe, expect, it } from 'vitest'` no-globals style + describe/it structure; do NOT use global test/expect)
    - .planning/phases/11-accounts-economy/11-PATTERNS.md §"src/lib/escapeHtml.ts" (lines 295-308) and §"test/unit/escape.test.ts" (lines 638-672) and §"test/unit/economy.test.ts" (lines 676-701) — exact source for all three files
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"XSS Hardening" (the 5-replacement esc body) and §"Economy Numbers" (constant rationale)
    - vitest.config.ts (confirm the `unit` project glob includes test/unit/*.test.ts)
  </read_first>
  <behavior>
    - esc('<b>') === '&lt;b&gt;'
    - esc('a & b') === 'a &amp; b'  (ampersand escaped FIRST so it does not double-escape)
    - esc('"val"') === '&quot;val&quot;'
    - esc("it's") === 'it&#39;s'
    - esc('hello') === 'hello'  (plain text unchanged)
    - esc('<script>alert(1)</script>') contains no raw '<'
    - economy: WIN_REWARD>0, LOSS_REWARD>0, LOSS_REWARD<WIN_REWARD, WELCOME_GRANT===unit cost (D-02), ceil(unit cost/WIN_REWARD) in [2,5] (D-03)
  </behavior>
  <action>
    Create src/lib/escapeHtml.ts exporting `esc(s: string): string` with exactly five chained
    `.replace()` calls in this order: `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;`, `"`→`&quot;`,
    `'`→`&#39;`. Ampersand MUST be first to avoid double-escaping.
    Create test/unit/escape.test.ts importing `{ esc }` from `../../src/lib/escapeHtml` and
    `{ describe, expect, it }` from 'vitest' (no globals), asserting the behaviors above.
    Create test/unit/economy.test.ts mirroring the SQL-side constants as local consts
    (WIN_REWARD=50, LOSS_REWARD=15, WELCOME_GRANT=100, UNIT_COST=100) and asserting the D-02/D-03/D-04
    relationships. These constants are display-only mirrors — note in a comment that the authoritative
    values live in the SQL RPCs (Plan 02) and the UI must never trust these for deduction (D-07).
  </action>
  <verify>
    <automated>npx vitest run --project unit test/unit/escape.test.ts test/unit/economy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project unit test/unit/escape.test.ts test/unit/economy.test.ts` exits 0 (GREEN)
    - src/lib/escapeHtml.ts contains exactly one exported `esc` and the `&` replacement appears before `<`
    - `npx tsc --noEmit` reports no new errors from escapeHtml.ts
  </acceptance_criteria>
  <done>Both unit test files pass; esc() handles all five entities and an XSS payload.</done>
</task>

<task type="auto">
  <name>Task 2: RED RLS scaffolds — inventory, settlement, migration</name>
  <files>test/rls/inventory-rls.test.ts, test/rls/settlement-idempotency.test.ts, test/rls/migration.test.ts</files>
  <read_first>
    - test/rls/wallet-rls.test.ts (the EXACT harness to copy: admin client with SERVICE key + persistSession:false; user client with ANON key; createUser via admin.auth.admin; seed via user.rpc('credit_wallet',...); forged-write assertion shape)
    - .planning/phases/11-accounts-economy/11-PATTERNS.md §"test/rls/inventory-rls.test.ts" (lines 539-590), §"test/rls/settlement-idempotency.test.ts" (lines 593-624, two-user setup), §"test/rls/migration.test.ts" (lines 627-635), §"RLS Test Harness Setup" (lines 750-758: fileParallelism:false already set)
    - .planning/phases/11-accounts-economy/11-VALIDATION.md §"Idempotency / Concurrency / Forged-Grant / Migration Matrix" (the authoritative scenario→assert→file map) and §"Per-Task Verification Map"
    - vitest.config.ts (confirm `rls` project + jsdom + fileParallelism:false)
  </read_first>
  <action>
    Create the three RLS test files using the wallet-rls.test.ts harness verbatim for client setup.
    These are RED scaffolds — they reference RPCs/columns Plan 02 creates, so they MUST fail now
    (RPC not found / column missing), NOT be skipped. Implement every scenario row from VALIDATION.md's
    matrix as a real `it(...)` with concrete assertions (do not leave `it.todo`):
    - inventory-rls.test.ts (one user, seed balance 100 via credit_wallet): forged direct
      `from('inventory').insert({ owner, unit_id:'assault_bot' })` leaves 0 rows (ECON-05); `spend_unlock('assault_bot')`
      deducts 100 + inserts one inventory row (ECON-03); `spend_unlock` at balance 50 returns
      `{ ok:false, reason:'insufficient_funds' }` (ECON-03); `Promise.all` of two concurrent
      `spend_unlock('assault_bot')` deducts exactly once, never negative, one inventory row (ECON-04).
    - settlement-idempotency.test.ts (two users A,B): both agree → A credited WIN_REWARD=50, B credited
      LOSS_REWARD=15 (ECON-01); double-submit same match_id credits once (ECON-04); re-submit after
      settled → `already_settled`, balance unchanged (ECON-04); concurrent second-reporters settle exactly
      once (ECON-04); mismatch (A says A, B says B) → match_settlements.voided=true, neither credited (ECON-04);
      lone report → `status:'pending'`, no settlement row, no credit (D-08/ECON-04); client cannot supply
      a reward amount — RPC signature only takes p_match_id + p_claimed_winner (ECON-02).
    - migration.test.ts (admin inserts a profiles row, then calls provision_account / runs backfill path):
      provision_account twice → one wallet row, welcome grant once, balance=100 (ACCT-04); existing player
      profile wins=5/losses=2/unlocked_units=['thorn_beast'] → after backfill wallet=100, inventory has
      'thorn_beast', wins/losses unchanged (ACCT-04); new account (no prior units) → wallet+welcome, empty
      inventory (ACCT-04).
    Use match_id as a freshly generated uuid per test. Reference RPCs by the names Plan 02 will create:
    `spend_unlock(p_unit_id text)`, `report_match_result(p_match_id uuid, p_claimed_winner uuid)`,
    `provision_account(p_user_id uuid)`.
  </action>
  <verify>
    <automated>npx vitest list --project rls test/rls/inventory-rls.test.ts test/rls/settlement-idempotency.test.ts test/rls/migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest list --project rls test/rls/inventory-rls.test.ts test/rls/settlement-idempotency.test.ts test/rls/migration.test.ts` enumerates every VALIDATION.md matrix scenario as a discrete test (no `it.todo`, no `describe.skip`)
    - `npx tsc -p tsconfig.test.json --noEmit` (or the configured test typecheck) passes — files are type-valid even though RPCs are not yet live
    - Each file imports SERVICE/ANON/URL only from `process.env.*`, never from `src/` (grep: `grep -rn "SUPABASE_SERVICE_ROLE_KEY" test/rls/ | grep -v "process.env"` returns nothing)
    - Running against a Supabase WITHOUT Plan 02's migration, these tests are RED (RPC/column missing) — confirming they are real targets, not vacuous passes
  </acceptance_criteria>
  <done>Three RLS test files exist, type-check, enumerate the full VALIDATION matrix, and are RED until Plan 02 lands the schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI env | SERVICE_ROLE key is read from CI env only; must never be imported from `src/` or bundled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | service_role key in test files | mitigate | Keys sourced exclusively from `process.env.*`; acceptance grep proves no literal/`src/` import |
| T-11-02 | Tampering | vacuous/skipped tests masking gaps | mitigate | Acceptance forbids `it.todo`/`describe.skip`; RED-until-Plan-02 requirement proves tests exercise real authority paths |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Phase 11 installs ZERO new packages (RESEARCH §Standard Stack); no Package Legitimacy Gate needed |
</threat_model>

<verification>
- `npx vitest run --project unit` — escape + economy GREEN.
- `npx vitest list --project rls` — all three new files enumerate the full matrix.
- `npx tsc --noEmit` — no new type errors from escapeHtml.ts.
</verification>

<success_criteria>
- esc() helper exists and is unit-proven against an XSS payload (D-14/ACCT-02).
- Economy constants asserted consistent with D-02/D-03/D-04.
- Three RLS test files are RED scaffolds covering the entire VALIDATION matrix — the executable contract Plan 02 must satisfy.
</success_criteria>

<output>
Create `.planning/phases/11-accounts-economy/11-01-SUMMARY.md` when done.
</output>
