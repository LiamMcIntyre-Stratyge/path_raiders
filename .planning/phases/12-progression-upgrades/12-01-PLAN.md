---
phase: 12-progression-upgrades
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/units/UnitData.ts
  - src/towers/TowerData.ts
  - src/lib/progression/clamp.ts
  - test/unit/progression/resolver.test.ts
  - test/unit/progression/clamp.test.ts
  - test/unit/progression/sim-levels.test.ts
  - test/rls/upgrades-rls.test.ts
autonomous: true
requirements: [PROG-03, PROG-04]
must_haves:
  truths:
    - "resolveUnitStats(id, 1) returns the exact flat UNITS hp/dmg for all 6 units (level-1 invariant)"
    - "resolveTowerStats(1).dmg equals the flat TOWER_DMG = 25 (level-1 invariant)"
    - "resolveUnitStats and resolveTowerStats clamp out-of-range levels (0, 999) to the nearest valid level"
    - "clampLevels clamps unit/tower levels to [1, MAX] and drops unknown unit ids"
    - "Wave-0 test files exist and the resolver/clamp suites pass GREEN; sim-levels and upgrades-rls suites exist RED (await later waves)"
  artifacts:
    - path: "src/units/UnitData.ts"
      provides: "UNIT_LEVELS per-level arrays, resolveUnitStats, MAX_UNIT_LEVEL, BALANCE_VERSION, UPGRADE_COSTS display mirror"
      contains: "resolveUnitStats"
    - path: "src/towers/TowerData.ts"
      provides: "TOWER_LEVELS per-level array, resolveTowerStats, MAX_TOWER_LEVEL, BALANCE_VERSION"
      contains: "resolveTowerStats"
    - path: "src/lib/progression/clamp.ts"
      provides: "clampLevels pure guard (D-12)"
      exports: ["clampLevels"]
    - path: "test/unit/progression/resolver.test.ts"
      provides: "level-1 invariant + out-of-range clamp tests"
    - path: "test/unit/progression/clamp.test.ts"
      provides: "clampLevels guard tests (D-12)"
    - path: "test/unit/progression/sim-levels.test.ts"
      provides: "RED scaffold for createWorld/spawnUnit stat injection (GREEN in plan 03)"
    - path: "test/rls/upgrades-rls.test.ts"
      provides: "RED scaffold for upgrade_spend RPC (GREEN in plan 02)"
  key_links:
    - from: "src/units/UnitData.ts::resolveUnitStats"
      to: "src/units/UnitData.ts::UNITS (flat baseline)"
      via: "level-1 invariant: UNIT_LEVELS[id][0] === flat UNITS value"
      pattern: "resolveUnitStats"
    - from: "src/lib/progression/clamp.ts"
      to: "src/units/UnitData.ts::MAX_UNIT_LEVEL, src/towers/TowerData.ts::MAX_TOWER_LEVEL"
      via: "imports MAX constants for clamp bounds"
      pattern: "MAX_(UNIT|TOWER)_LEVEL"
---

<objective>
Build the pure progression foundation: per-level stat tables, the two resolver functions, the clamp guard, and all Wave-0 test scaffolds.

Purpose: Every downstream plan composes around two pure functions — `resolveUnitStats(unitId, level) → { hp, dmg }` and `resolveTowerStats(level) → { dmg, range, maxCd }` — plus the `clampLevels` guard. Defining these contracts first (with the level-1 invariant locked by test) prevents silent stat regressions and gives plans 02/03/04 a stable seam. This plan also lays the RED test scaffolds that plans 02 and 03 turn GREEN (Nyquist Wave-0 gap closure).

Output: Extended `UnitData.ts`/`TowerData.ts` (per-level arrays, resolvers, BALANCE_VERSION, display-only UPGRADE_COSTS), new `src/lib/progression/clamp.ts`, and 4 test files. The resolver and clamp suites pass GREEN; the sim-levels and upgrades-rls suites exist and are RED (their production code lands in plans 03 and 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-progression-upgrades/12-CONTEXT.md
@.planning/phases/12-progression-upgrades/12-PATTERNS.md
@.planning/phases/12-progression-upgrades/12-RESEARCH.md
@.planning/phases/12-progression-upgrades/12-VALIDATION.md

<interfaces>
<!-- Contracts this plan DEFINES for downstream plans. Authoritative shapes — do not re-explore. -->

resolveUnitStats(unitId: string, level: number): { hp: number; dmg: number }
resolveTowerStats(level: number): { dmg: number; range: number; maxCd: number }
clampLevels(rawUnitLevels: Record<string, number>, rawTowerLevel: number): { unitLevels: Record<string, number>; towerLevel: number }

UNIT_LEVELS: Record<string, { hp: number; dmg: number }[]>  // index = level - 1
TOWER_LEVELS: { dmg: number; range: number; maxCd: number }[]  // index = level - 1
MAX_UNIT_LEVEL = 5, MAX_TOWER_LEVEL = 5, BALANCE_VERSION = 1
UPGRADE_COSTS: { unit: { 2:75,3:150,4:300,5:600 }, tower: { 2:100,3:200,4:400,5:800 } }  // display-only mirror

Level-1 invariant (locked by test): UNIT_LEVELS[id][0] === flat UNITS value for all 6 ids; TOWER_LEVELS[0].dmg === TOWER_DMG (25).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend UnitData.ts and TowerData.ts with per-level tables + resolvers + write resolver tests (RED→GREEN)</name>
  <files>src/units/UnitData.ts, src/towers/TowerData.ts, test/unit/progression/resolver.test.ts</files>
  <read_first>
    - src/units/UnitData.ts (the flat UNITS array — its hp/dmg values are the locked level-1 baseline)
    - src/towers/TowerData.ts (the flat TOWER_RANGE=216, TOWER_DMG=25, TOWER_CD=1400 constants — level-1 baseline)
    - .planning/phases/12-progression-upgrades/12-PATTERNS.md (sections "src/towers/TowerData.ts" and "src/units/UnitData.ts" — exact append-after-export blocks with authored per-level numbers; section "test/unit/progression/resolver.test.ts")
    - test/unit/economy.test.ts (display-constant assertion pattern, describe/it nesting)
  </read_first>
  <behavior>
    - resolveUnitStats(id, 1).hp === flat UNITS hp and .dmg === flat UNITS dmg, for all 6 unit ids (level-1 invariant).
    - resolveTowerStats(1).dmg === TOWER_DMG (25); resolveTowerStats(1).range === TOWER_RANGE; resolveTowerStats(1).maxCd === TOWER_CD.
    - resolveUnitStats('scout_drone', 0) deep-equals resolveUnitStats('scout_drone', 1) (low clamp).
    - resolveUnitStats('scout_drone', 999) deep-equals resolveUnitStats('scout_drone', MAX_UNIT_LEVEL) (high clamp).
    - resolveUnitStats('unknown_id', 3) returns the flat-UNITS fallback (no crash) — and for a truly absent id, a safe default.
    - UNIT_LEVELS[id] has exactly MAX_UNIT_LEVEL (5) entries for every unit; TOWER_LEVELS has exactly MAX_TOWER_LEVEL (5) entries.
  </behavior>
  <action>
Append to `src/towers/TowerData.ts` (no new imports; keep flat TOWER_RANGE/TOWER_DMG/TOWER_CD exports unchanged): `BALANCE_VERSION = 1` (D-07), `interface TowerLevelStats { dmg; range; maxCd }`, `MAX_TOWER_LEVEL = 5` (D-10), `TOWER_LEVELS: TowerLevelStats[]` with exactly 5 entries where index 0 = `{ dmg: 25, range: TOWER_RANGE, maxCd: TOWER_CD }` and dmg escalates per the 12-PATTERNS authored curve (25/32/41/52/65) with range/maxCd held at TOWER_RANGE/TOWER_CD for every level (D-02/D-06 — author range/cd per level for uniform shape even though only dmg scales), and `resolveTowerStats(level)` returning `TOWER_LEVELS[Math.max(0, Math.min(level-1, TOWER_LEVELS.length-1))]`.

Append to `src/units/UnitData.ts` (no new imports; keep flat UNITS array unchanged): `BALANCE_VERSION = 1`, `interface UnitLevelStats { hp; dmg }` (speedPx/attackRate excluded — fixed per D-05), `MAX_UNIT_LEVEL = 5`, `UNIT_LEVELS: Record<string, UnitLevelStats[]>` for all 6 ids (scout_drone, assault_bot, vine_crawler, thorn_beast, apprentice_mage, elementalist) using the exact authored per-level numbers from 12-PATTERNS where each id's index-0 entry equals its flat UNITS hp/dmg (the level-1 invariant), and `resolveUnitStats(unitId, level)` that returns the flat-UNITS fallback for an unknown id and otherwise `UNIT_LEVELS[id][Math.max(0, Math.min(level-1, len-1))]`. Also append the display-only `UPGRADE_COSTS = { unit: {2:75,3:150,4:300,5:600}, tower: {2:100,3:200,4:400,5:800} }` mirror (D-13 curve, calibrated against the P11 economy WIN=50/LOSS=15/welcome=100/unit-unlock=100 so early upgrades are a few wins and L5 is a genuine long-tail; authoritative copy lives in the upgrade_spend RPC, plan 02 — this is display-only, never supplied to the RPC, per D-03/PROG-04).

Write `test/unit/progression/resolver.test.ts` covering the <behavior> assertions, iterating the flat UNITS array for the level-1 invariant and asserting array lengths === MAX. This task is RED→GREEN: tests and implementation land together and must end GREEN.
  </action>
  <verify>
    <automated>npx vitest run --project unit -- resolver</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project unit -- resolver` passes GREEN.
    - Source assertion: `src/units/UnitData.ts` exports `resolveUnitStats`, `UNIT_LEVELS`, `MAX_UNIT_LEVEL`, `BALANCE_VERSION`, `UPGRADE_COSTS`; `src/towers/TowerData.ts` exports `resolveTowerStats`, `TOWER_LEVELS`, `MAX_TOWER_LEVEL`, `BALANCE_VERSION`.
    - Behavior assertion: the level-1 invariant test exists and passes for all 6 units AND for the tower; the flat UNITS array and TOWER_DMG/RANGE/CD constants are unchanged (no edits to existing values).
  </acceptance_criteria>
  <done>resolver.test.ts GREEN; per-level tables + resolvers + BALANCE_VERSION + UPGRADE_COSTS added without altering any existing flat baseline value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add clampLevels guard + its tests (RED→GREEN)</name>
  <files>src/lib/progression/clamp.ts, test/unit/progression/clamp.test.ts</files>
  <read_first>
    - src/lib/sideHelper.ts (the pure-utility-module analog: named exports, zero Phaser/Supabase imports, JSDoc)
    - .planning/phases/12-progression-upgrades/12-PATTERNS.md (section "src/lib/progression/clamp.ts" — full pattern; section "test/unit/progression/clamp.test.ts")
    - src/units/UnitData.ts (for MAX_UNIT_LEVEL import — created in Task 1)
    - src/towers/TowerData.ts (for MAX_TOWER_LEVEL import — created in Task 1)
  </read_first>
  <behavior>
    - clampLevels({ scout_drone: 999 }, 9) → { unitLevels: { scout_drone: 5 }, towerLevel: 5 }.
    - clampLevels({ scout_drone: -5 }, 0) → { unitLevels: { scout_drone: 1 }, towerLevel: 1 }.
    - clampLevels({ unknown_unit: 3 }, 2) → unitLevels has no 'unknown_unit' key (silently dropped).
    - clampLevels({ scout_drone: 3, vine_crawler: 2 }, 4) → values pass through unchanged.
    - Non-integer inputs are floored (Math.floor).
  </behavior>
  <action>
Create `src/lib/progression/clamp.ts` mirroring `sideHelper.ts` style: import `MAX_UNIT_LEVEL` from `../units/UnitData` and `MAX_TOWER_LEVEL` from `../towers/TowerData`; define a module-level `KNOWN_UNIT_IDS` Set of the 6 unit ids; export `clampLevels(rawUnitLevels, rawTowerLevel)` that iterates `Object.entries(rawUnitLevels)`, keeps only ids in `KNOWN_UNIT_IDS`, clamps each kept level via `Math.max(1, Math.min(MAX_UNIT_LEVEL, Math.floor(level)))`, drops unknown ids, and clamps towerLevel via `Math.max(1, Math.min(MAX_TOWER_LEVEL, Math.floor(rawTowerLevel)))`. Zero Supabase/Phaser/gameState imports (D-12 / sim-purity spirit). Write `test/unit/progression/clamp.test.ts` covering the <behavior> cases. RED→GREEN: end GREEN.
  </action>
  <verify>
    <automated>npx vitest run --project unit -- clamp</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project unit -- clamp` passes GREEN.
    - Source assertion: `src/lib/progression/clamp.ts` exports `clampLevels` and imports only `MAX_UNIT_LEVEL`/`MAX_TOWER_LEVEL` (no Supabase/Phaser imports — grep the file: `grep -v '^//' src/lib/progression/clamp.ts | grep -c -E "supabase|phaser" == 0`).
    - Behavior assertion: unknown-id-drop and both clamp bounds proven by passing tests.
  </acceptance_criteria>
  <done>clamp.ts pure module created; clamp.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Author RED scaffolds for sim-levels (plan 03) and upgrades-rls (plan 02)</name>
  <files>test/unit/progression/sim-levels.test.ts, test/rls/upgrades-rls.test.ts</files>
  <read_first>
    - test/unit/sim/_helpers.ts (makeWorld / makeBase / makeOver factories — the world factory the sim-levels test extends)
    - test/unit/sim/movement.test.ts (sim behavior test structure)
    - test/rls/inventory-rls.test.ts (the verbatim structural template for upgrades-rls: makeAdmin, seedUser, credit_wallet seed via RPC, admin re-read helpers, asSpend cast, Promise.all concurrency)
    - test/rls/helpers.ts (seedUser/makeAdmin/mintToken signatures)
    - .planning/phases/12-progression-upgrades/12-PATTERNS.md (sections "test/unit/progression/sim-levels.test.ts" and "test/rls/upgrades-rls.test.ts")
    - .planning/phases/12-progression-upgrades/12-VALIDATION.md (the per-requirement test rows these files must satisfy)
  </read_first>
  <action>
Create `test/unit/progression/sim-levels.test.ts` asserting PROG-03 sim injection: a `makeWorldWithLevels` helper wrapping `createWorld` (passing the new `hostTowerLevel`/`guestTowerLevel`/`hostUnitLevels`/`guestUnitLevels` fields), a test that `createWorld({ hostTowerLevel: 3 })` makes host-side towers carry `TOWER_LEVELS[2].dmg`, and a test that deploying a unit at level 2 via `spawnUnit` yields `unit.hp`/`unit.dmg` matching `resolveUnitStats(id, 2)`. These reference the plan-03 production fields that do not exist yet → the suite is RED now (compile/assertion failure is expected and acceptable for this scaffold task). Do NOT stub or weaken assertions to force green.

Create `test/rls/upgrades-rls.test.ts` replicating `inventory-rls.test.ts` structure for the `upgrade_spend` RPC and `upgrades` table (both created in plan 02): module-level `makeAdmin()`, `beforeAll` seeding a user via `seedUser(admin, 'tupg')` + `credit_wallet` RPC (namespaced idempotency key) + an `admin.from('inventory').insert({ owner: userId, unit_id: 'scout_drone' })` ownership fixture; `balanceOf` and `levelOf(id, scope, targetId)` admin re-read helpers (absent row → level 1); an `asUpgrade` cast helper. Cover the VALIDATION rows: forged direct INSERT denied by RLS, owned-unit upgrade deducts cost + increments level, insufficient_funds, unowned-unit reject (D-16), unknown unit id reject, max-level (5→6) reject, concurrent upgrade deducts exactly once (Promise.all), and scope='tower'/'tower_power' increment (PROG-02). RED now (table/RPC absent).

Mark both files clearly as Wave-0 RED scaffolds (a top-of-file comment naming the plan that turns each GREEN: sim-levels → plan 03, upgrades-rls → plan 02).
  </action>
  <verify>
    <automated>npx vitest run --project unit -- progression 2>&1 | grep -qE "sim-levels|fail|error" && echo "sim-levels scaffold present (RED expected)"; test -f test/rls/upgrades-rls.test.ts && echo "upgrades-rls scaffold present"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: `test -f test/unit/progression/sim-levels.test.ts && test -f test/rls/upgrades-rls.test.ts`.
    - Source assertion: `sim-levels.test.ts` references `hostTowerLevel` and `resolveUnitStats`; `upgrades-rls.test.ts` references `upgrade_spend`, `upgrades`, and the 8 behaviors above (grep each behavior's `it(` description).
    - These suites are RED by design (production code lands in plans 02/03). The unit-project run for the resolver+clamp suites (Tasks 1-2) still passes; sim-levels failing is expected and must NOT be masked.
  </acceptance_criteria>
  <done>Both RED scaffolds authored with real assertions (no stubs); plan 02 turns upgrades-rls GREEN, plan 03 turns sim-levels GREEN.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project unit -- resolver` GREEN (level-1 invariant + clamp).
- `npx vitest run --project unit -- clamp` GREEN.
- Flat UNITS array + TOWER_DMG/RANGE/CD unchanged (no silent stat regression).
- sim-levels.test.ts and upgrades-rls.test.ts exist as RED scaffolds with real assertions.
- `npx tsc --noEmit` (prod tsconfig) still clean for the modified data modules (test files use tsconfig.test.json).
</verification>

<success_criteria>
- Two pure resolvers + clamp guard exist, fully tested, with the level-1 invariant locked.
- BALANCE_VERSION and the levels-not-stats table shape satisfy the PROG-04 retune-safely seam (D-03/D-07).
- All four Wave-0 test files exist; resolver/clamp GREEN, sim-levels/upgrades-rls RED awaiting plans 03/02.
</success_criteria>

<output>
Create `.planning/phases/12-progression-upgrades/12-01-SUMMARY.md` when done.
</output>
