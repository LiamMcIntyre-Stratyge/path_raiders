---
phase: 10-services-simulation-refactor
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - test/unit/sim/win.test.ts
  - test/unit/sim/wall-break.test.ts
  - test/unit/sim/snapshot.test.ts
autonomous: false
requirements: [BATTLE-02]
must_haves:
  truths:
    - "Win-by-base-reach and win-by-timer-expiry are unit-tested in the Phase 9 harness"
    - "Wall-break detour (pathfinder integration) is unit-tested end-to-end through the sim"
    - "A characterization snapshot locks a scripted-battle trace as a forward regression net"
    - "The full vitest suite is green (Phase 9 + all five D-17/D-15 sim scenarios)"
    - "The no-player-visible-change parity gate passes the manual two-session playtest"
  artifacts:
    - path: "test/unit/sim/win.test.ts"
      provides: "D-17c: win by base-reach AND timer expiry"
      contains: "game_over"
    - path: "test/unit/sim/wall-break.test.ts"
      provides: "D-17d: wall-break detour via pathfinder"
      contains: "wall_break"
    - path: "test/unit/sim/snapshot.test.ts"
      provides: "D-15: characterization snapshot regression lock"
      contains: "toMatchSnapshot"
  key_links:
    - from: "test/unit/sim/wall-break.test.ts"
      to: "src/lib/pathfinder.ts"
      via: "detour recompute through findPath"
      pattern: "wall"
    - from: "test/unit/sim/snapshot.test.ts"
      to: "src/sim/step.ts"
      via: "scripted dt/rng/deploy loop snapshotted"
      pattern: "toMatchSnapshot"
---

<objective>
Complete the simulation test coverage (D-17 c/d + D-15) and run the parity gate (D-16). Add the win-condition tests (base-reach and timer-expiry), the wall-break detour test (pathfinder integration), and the characterization snapshot that locks a scripted-battle trace as a forward regression net. Then execute the manual two-session host+guest playtest that is the authoritative "no player-visible behavior change" parity gate.

Per RESEARCH.md recommended sequence steps 7, 9, 10: this plan finalizes the forward regression net and runs the manual parity check. The unit tests are the forward regression net, NOT the old-vs-new proof — a frame-identical automated diff vs the old GameScene is impractical (Phaser entanglement + variable dt), so parity is confirmed by the manual playthrough (D-16, the project's current verification method per TESTING.md).

Purpose: Establishes the full BATTLE-02 unit-test coverage in the Phase 9 harness and confirms the behavior-preserving refactor actually preserved behavior before the phase is marked complete.
Output: `test/unit/sim/{win,wall-break,snapshot}.test.ts` + committed snapshot fixture, and a recorded manual-parity sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-services-simulation-refactor/10-CONTEXT.md
@.planning/phases/10-services-simulation-refactor/10-RESEARCH.md
@.planning/phases/10-services-simulation-refactor/10-PATTERNS.md
@.planning/phases/10-services-simulation-refactor/10-VALIDATION.md
@.planning/phases/10-services-simulation-refactor/10-02-SUMMARY.md
@.planning/phases/10-services-simulation-refactor/10-03-SUMMARY.md

<interfaces>
<!-- From Plans 02-04 (now in codebase) + the existing test scaffold. -->

From src/sim/world.ts: createWorld(opts), spawnUnit(world, input, events)
From src/sim/step.ts: step(world, inputs, dt, rng = Math.random): SimEvent[]
From test/unit/sim/_helpers.ts (Plan 02): makeBase/makeOver/paintOver, spawnTestUnit

Win conditions (RESEARCH.md Q7c):
  - base-reach: a unit at wpIdx >= waypoints.length reaching the enemy base → base_hit event,
    then base HP 0 → game_over (winner = side that still has HP)
  - timer: set world.timeLeft small, step past it → game_over with winner by hostBaseHp vs guestBaseHp
    comparison (host>guest→host, guest>host→guest, equal→tie)

Wall-break detour (RESEARCH.md Q7d, reuses pathfinder test scaffold):
  - spawn a unit whose path crosses a breakable wall → unit sets wallTarget → attacks → wall hp reaches 0
    → wall_break event → mutableOver cleared → path recomputed (detour). canBreakWall gates by faction.

Characterization snapshot (RESEARCH.md Q7 + Pitfall 2):
  - scripted ~10s battle: fixed dt=16ms loop, fixed rng=()=>0.5, known deploys → snapshot final world.
  - world.wallHP is a Map → serialize with a replacer: val instanceof Map ? Array.from(val.entries()).sort() : val
  - expect(serialized).toMatchSnapshot(); committed under __snapshots__/; re-lock via `vitest --update-snapshots`

Test analog: test/unit/pathfinder.test.ts (describe/it + makeBase/makeOver). Run: npx vitest run test/unit/sim

Manual parity gate (D-16, TESTING.md): host+guest two-session playtest — same battle outcomes/feel as v1.0;
  practice-mode AI battle plays identically. The ONE accepted micro-change is the D-07 id-tiebreak
  (sub-pixel distance ties only — effectively invisible).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Win-condition + wall-break detour tests (D-17 c/d)</name>
  <files>test/unit/sim/win.test.ts, test/unit/sim/wall-break.test.ts</files>
  <read_first>
    - test/unit/sim/_helpers.ts (Plan 02 scaffold — makeBase/makeOver/spawnTestUnit)
    - test/unit/sim/combat.test.ts (Plan 02 — the established describe/it + step-loop pattern)
    - src/sim/step.ts + src/sim/combat.ts (the win/base-reach/wall logic under test)
    - src/lib/pathfinder.ts + test/unit/pathfinder.test.ts (findPath + canBreakWall; the grid-with-wall setup to replicate)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (D-17 scenario stubs for win.test.ts and wall-break.test.ts)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Q7 c/d implementations)
  </read_first>
  <behavior>
    - win.test.ts (timer): with world.timeLeft set just above 0, one step past it emits a `game_over` event; winner matches the hostBaseHp vs guestBaseHp comparison (host>guest→host, equal→tie)
    - win.test.ts (base-reach): a unit advanced to/past its final waypoint at the enemy base emits a `base_hit` event; when base HP hits 0 a `game_over` event with the correct winner is emitted
    - wall-break.test.ts (target): a unit whose next waypoint is a breakable wall (canBreakWall true for its faction) sets wallTarget instead of moving through
    - wall-break.test.ts (break + detour): repeated steps drive wall hp to 0 → a `wall_break` event is emitted, mutableOver at that cell is cleared, and the unit's path is recomputed (detour) so it resumes toward the base
  </behavior>
  <action>
    Write `test/unit/sim/win.test.ts` replicating the combat.test.ts pattern. Timer case: createWorld, set world.timeLeft to a small value (e.g. 0.001), call step(world, [], 16, ()=>0), assert an event with type 'game_over' is returned and its winner matches the base-HP comparison; add a tie case (equal base HP) and a host-wins case (manually lower guestBaseHp). Base-reach case: spawn a unit positioned at its final waypoint adjacent to the enemy base (or advance it via steps until isAtGoal with waypoints exhausted), step until a 'base_hit' event appears; drive the targeted base HP to 0 (either via repeated reaches or by presetting low HP) and assert a 'game_over' event with the attacker's side as winner. Write `test/unit/sim/wall-break.test.ts` reusing the pathfinder test's grid-with-wall setup (makeBase/makeOver + paintOver a 'wall' or 'break_*' cell on the unit's lane, with the unit's faction able to break it per canBreakWall). Assert: (target) after the unit reaches the wall cell, wallTarget is set; (break) stepping until the accumulated wall damage reaches the wall's max HP emits a 'wall_break' event, world.mutableOver[r][c] becomes null, and the unit subsequently advances past the cell (path recomputed). These tests MUST pass against the already-implemented sim (no new sim code — if a test reveals a behavior gap, fix the sim in src/sim/ and note it in the SUMMARY).
  </action>
  <verify>
    <automated>npx vitest run test/unit/sim/win.test.ts test/unit/sim/wall-break.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - win.test.ts asserts both timer-expiry and base-reach game_over paths (grep -c "game_over" test/unit/sim/win.test.ts >= 2; a tie case present)
    - wall-break.test.ts asserts wall_break event + mutableOver cleared + detour (grep `wall_break` and `mutableOver` present)
    - Both files pass: `npx vitest run test/unit/sim/win.test.ts test/unit/sim/wall-break.test.ts` exits 0
    - `npx tsc --noEmit -p tsconfig.test.json` exits 0
  </acceptance_criteria>
  <done>Win-by-timer, win-by-base-reach, and wall-break detour scenarios are green in the Phase 9 harness; D-17 c/d covered.</done>
</task>

<task type="auto">
  <name>Task 2: Characterization snapshot regression lock (D-15)</name>
  <files>test/unit/sim/snapshot.test.ts</files>
  <read_first>
    - test/unit/sim/_helpers.ts + win.test.ts/combat.test.ts (Task 1 + Plan 02 — established harness)
    - src/sim/step.ts, src/sim/world.ts (the scripted loop drives these)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (snapshot.test.ts stub — serializeWorld replacer for Map stability)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (D-15 characterization snapshot; Pitfall 2 Map iteration stability)
  </read_first>
  <action>
    Write `test/unit/sim/snapshot.test.ts` that drives a deterministic scripted battle: createWorld with fixed opts, a fixed rng `() => 0.5`, and a fixed dt of 16ms; run a loop (~10000ms total → ~625 steps) injecting a known, scripted set of deploy inputs at fixed tick offsets (e.g. a host deploy at tick 10, a guest deploy at tick 20 — choose deterministic, documented inputs). After the loop, serialize the final world to a stable string using a JSON replacer that converts any Map to `Array.from(val.entries()).sort()` (Pitfall 2 — raw JSON.stringify drops Maps and is non-deterministic). Assert `expect(serialized).toMatchSnapshot()`. Run once to generate the committed snapshot under `test/unit/sim/__snapshots__/`. Treat snapshot mismatch as a blocking failure (default Vitest behavior); document in the SUMMARY that `npx vitest run --update-snapshots` is the intentional re-lock command for future intentional sim changes. Commit the generated snapshot file.
  </action>
  <verify>
    <automated>npx vitest run test/unit/sim/snapshot.test.ts && npx vitest run test/unit/sim/snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - snapshot.test.ts uses a fixed rng (`() => 0.5`), fixed dt (16), and a scripted deploy sequence (grep `() => 0.5` and `toMatchSnapshot` present)
    - The Map-stable replacer is present (grep `instanceof Map` and `.sort()` present)
    - A committed snapshot exists under test/unit/sim/__snapshots__/ after first run
    - Running the test TWICE in a row both pass (determinism: the double-run verify command exits 0) — proves the snapshot is stable, not flaky
  </acceptance_criteria>
  <done>A deterministic scripted-battle snapshot is committed and stable across repeated runs; the forward regression lock (D-15) is in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 10 sim extraction: the battle loop now runs in pure src/sim/ (Plans 02-03), gameState is slimmed to a session/profile cache (Plan 04), towers live in src/towers/ (Plan 01), and the complete D-17/D-15 unit-test suite is green (Plans 02 + this plan). All automated checks pass: `npx tsc --noEmit -p tsconfig.json` and `npx vitest run` are green.
  </what-built>
  <how-to-verify>
    This is the D-16 parity gate — the authoritative "no player-visible behavior change" check (an automated old-vs-new diff is impractical per D-16). Run the project's standard two-session manual playtest:

    1. Start the dev server (e.g. `npm run dev`) and open two browser sessions.
    2. Host one match and join as guest from the second session (room-code path).
    3. Play a full battle: deploy units across all 3 slots for both factions; let units fight; break at least one wall; reach an enemy base; and let a match end by base destruction. Start a second match and let it end by timer expiry.
    4. Also run one practice-mode match (AI opponent) to verify AI spawning still works.
    5. Confirm against v1.0 feel: gold accrues at the same rate (+10/2s, cap 9999), the 180s timer counts down, units move/fight/die with the same animations, towers fire, walls break and units detour, base HP drops and flashes, the result overlay shows correct winner + base HP, and wins/unlocks still record (non-practice).
    6. The ONLY accepted micro-change is the D-07 id-tiebreak — it affects only exact sub-pixel distance ties in target selection and is effectively invisible. Everything else must look and feel identical to v1.0.

    If anything differs (other than the flagged tiebreak), describe the divergence so the sim can be corrected before the phase is marked complete.
  </how-to-verify>
  <resume-signal>Type "approved" if the two-session + practice playtest matches v1.0 behavior (modulo the D-07 tiebreak), or describe the divergences observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only test files + a manual playtest. No production code surface, no I/O, no networking changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | test-only changes | accept | Tests and the snapshot fixture introduce no runtime/external surface. The manual parity gate strengthens assurance that the refactor did not weaken any existing trust boundary (sim stays transport-free; scene keeps owning networking; no client-authority added/removed — server-authority hardening is deferred to Phase 11/14). |

**Honest assessment:** Test-and-verify plan. No new external surface, no new I/O. The parity gate is a security-relevant assurance step (confirms boundaries unchanged), not a new surface.
</threat_model>

<verification>
- `npx vitest run` — full suite green: Phase 9 tests + all five sim scenarios (movement, combat, win, wall-break, snapshot)
- `npx tsc --noEmit -p tsconfig.json` and `-p tsconfig.test.json` both exit 0
- Snapshot stable across repeated runs (no flakiness)
- Manual two-session + practice playtest sign-off (D-16 parity gate) — the authoritative no-player-visible-change confirmation
</verification>

<success_criteria>
- ROADMAP SC#2 complete: the extracted simulation has unit tests in the Phase 9 harness covering core combat/movement/win resolution (all of D-17 a-d + the D-15 snapshot)
- D-15 (characterization snapshot), D-17 (all four scenario coverage), D-16 (manual parity gate) all satisfied
- Phase 10 behavior-preserving claim confirmed by manual playtest; the only accepted micro-change is the D-07 id-tiebreak
- Full suite tsc-green and vitest-green before `/gsd:verify-work`
</success_criteria>

<output>
Create `.planning/phases/10-services-simulation-refactor/10-05-SUMMARY.md` when done.
</output>
