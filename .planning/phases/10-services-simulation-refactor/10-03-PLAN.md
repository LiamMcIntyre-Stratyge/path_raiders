---
phase: 10-services-simulation-refactor
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/units/UnitView.ts
  - src/scenes/GameScene.ts
  - src/units/Unit.ts
autonomous: true
requirements: [BATTLE-02]
must_haves:
  truths:
    - "GameScene.update() drives step(world, inputs, dt, Math.random) instead of its own five updateX methods"
    - "Unit rendering is a UnitView keyed by SimUnit.id, reconciled by id each frame (not array position)"
    - "Discrete sim events (unit_died, wall_break, base_hit, game_over) drive SFX/animation/camera/network in the scene"
    - "The Supabase wire protocol is preserved byte-for-byte (deploy, wall_break, base_hp, game_over)"
    - "The sim never imports supabase; the scene owns all networking"
  artifacts:
    - path: "src/units/UnitView.ts"
      provides: "Phaser Container render half of the unit, keyed by id, with syncFrom + playDeathAnimation"
      contains: "syncFrom"
    - path: "src/scenes/GameScene.ts"
      provides: "Thin renderer: step() + reconcile + event mapping + network"
      contains: "step("
  key_links:
    - from: "src/scenes/GameScene.ts"
      to: "src/sim/step.ts"
      via: "step(world, inputs, dt) in update()"
      pattern: "step\\(this\\.world"
    - from: "src/scenes/GameScene.ts"
      to: "src/units/UnitView.ts"
      via: "reconcile by SimUnit.id"
      pattern: "syncFrom"
    - from: "src/scenes/GameScene.ts"
      to: "supabase channel"
      via: "broadcast events mapped to/from sim inputs/events (unchanged protocol)"
      pattern: "event: 'deploy'"
---

<objective>
Wire `GameScene` to the pure sim built in Plan 02: split `Unit.ts` into a `UnitView` render-half (D-02), replace the five `updateX()` calls with a single `step()` call, reconcile views by `SimUnit.id` each frame (D-03), map discrete sim events to SFX/animation/camera/network, and preserve the Supabase wire protocol byte-for-byte (D-04). After this plan `GameScene` is a thin renderer + network + input layer; the sim is the single source of truth for live battle state.

Per RESEARCH.md recommended sequence step 6: this is the integration step. The riskiest for SC#1 (no player-visible change) — every event mapping and network broadcast must reproduce today's behavior exactly. The parity gate is the phase-end manual two-session playtest (D-16, executed in Plan 05), not an automated diff.

Purpose: Completes the GameScene→sim decoupling so battle physics, networking, and rendering are separable. Sets up Phase 14 to swap rng/timestep and add report submission at the same call sites.
Output: `src/units/UnitView.ts` (new), a thinned `GameScene.ts` driving the sim, and `Unit.ts` reduced/re-exported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-services-simulation-refactor/10-CONTEXT.md
@.planning/phases/10-services-simulation-refactor/10-RESEARCH.md
@.planning/phases/10-services-simulation-refactor/10-PATTERNS.md
@.planning/phases/10-services-simulation-refactor/10-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (now in codebase) + the live GameScene seams. -->

From src/sim/step.ts: step(world, inputs: SimInput[], dt, rng = Math.random): SimEvent[]
From src/sim/world.ts: createWorld(opts), spawnUnit(world, input, events)
From src/sim/types.ts: SimUnit, SimWorld, SimEvent, SimInput (see Plan 02)

SimEvent union (what the scene consumes):
  {type:'unit_died';unitId;x;y;faction} | {type:'wall_break';row;col} |
  {type:'base_hit';side;newHp} | {type:'game_over';winner}

Reconcile pattern (D-03, from 10-PATTERNS.md "Reconcile pattern"):
  build a Set of live ids from [...hostUnits, ...guestUnits]; create UnitView for new ids (popIn);
  view.syncFrom(u) for existing; for views whose id is no longer live → playDeathAnimation() + delete.
  KEY: reconcile by SimUnit.id, NEVER array position (order changes on prune).

UnitView (the Phaser render-half to extract from Unit.ts):
  constructor(scene, id: string, def: UnitDefinition, laneSlot, dir) — same Phaser Container setup as Unit.ts:30-56
  syncFrom(u: SimUnit): update x/y, redraw HP bar when hp changed
  playDeathAnimation(): the kill() explosion+fade tween from Unit.ts:114-144
  Keep: flashHit, popIn, drawHP. DROP from view: hp/attackCd/waypoints/wpIdx/wallTarget (now sim-owned),
  moveStep/isAtGoal/takeDamage (moved to sim in Plan 02).

GameScene.update today (:386-398) — the seam to replace:
  updateGold/updateTimer/updateAI/updateUnits/updateTowers + prune → ONE step() + reconcile + event loop.

Wire protocol (PRESERVE EXACTLY — GameScene :371-377, :565-568, :589-591, :827-866):
  deploy  {unitId, slot, role}  — sent after local deploy; received → push into sim inputs queue
  wall_break {row, col}         — sent after sim wall_break event (non-practice); received → apply to world
  base_hp {side, hp}            — sent after sim base_hit event; received → directly set world.hostBaseHp/guestBaseHp
  game_over {winner}            — sent after sim game_over event; received → triggerGameOver(winner)

Landmines (RESEARCH.md Q8 + Pitfalls):
  - base_hp received: overwrite world.hostBaseHp/guestBaseHp (NOT gameState — that field is removed in Plan 04; for now write world)
  - triggerGameOver idempotency: guard with world.over; renderer calls recordResult once via events.some(e=>e.type==='game_over')
  - audio.playHit per-attack: keep in scene by reading attackCd resets as continuous state, OR accept it fires on combat — do NOT emit a per-hit event (Q2 open question recommendation)
  - gameState.gold deploy deduction: deploy now deducts from world.gold; affordability reads world.gold (Plan 04 removes gameState.gold — keep consistent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split Unit.ts → src/units/UnitView.ts (render-half, keyed by id, D-02)</name>
  <files>src/units/UnitView.ts, src/units/Unit.ts</files>
  <read_first>
    - src/units/Unit.ts (full — the class to split; keep Phaser half, drop the now-sim-owned data/logic)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (section "src/units/UnitView.ts" — constructor, syncFrom, playDeathAnimation, kept view methods)
    - src/sim/types.ts (SimUnit — what syncFrom reads from)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Q2 unit field split; Anti-pattern: using Unit.takeDamage to trigger death from sim)
  </read_first>
  <action>
    Create `src/units/UnitView.ts` exporting a `UnitView extends Phaser.GameObjects.Container` whose constructor takes `(scene, id: string, def: UnitDefinition, laneSlot: number, dir: 1|-1)` and does the same Phaser setup as Unit.ts:30-56 (token image with setDisplaySize(36,36), flipY when dir===1, hpGfx graphics, drawHP, add.existing, setDepth(10)). Store the `id` for reconcile. Keep the pure-Phaser view methods from Unit.ts: `flashHit()` (:96-101), `popIn()` (:103-110), and rename the private `kill()` (:114-144) to a public `playDeathAnimation()` (the explosion burst + fade tween + destroy). Add `syncFrom(u: SimUnit)`: set this.x=u.x, this.y=u.y, and redraw the HP bar when u.hp changed (track a `_lastHp` field; drawHP takes hp/maxHp from the SimUnit). The HP bar drawHP logic is unchanged from Unit.ts:146-155. DROP everything now sim-owned: hp/maxHp/attackCd/attackRate/waypoints/wpIdx/wallTarget fields and the moveStep/isAtGoal/takeDamage/setWaypoints methods (those moved to the sim in Plan 02). Reduce `src/units/Unit.ts` to a thin re-export of COMBAT_RANGE/BASE_REACH_DMG if anything outside still imports them, OR delete it and update importers — Claude's discretion, but the build must stay tsc-green (the sim now owns COMBAT_RANGE/BASE_REACH_DMG per Plan 02). Do NOT wire reconcile into GameScene yet (Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/units/UnitView.ts` exists, extends Phaser.GameObjects.Container, exports `UnitView` with `syncFrom` and `playDeathAnimation` (grep all present)
    - UnitView no longer carries sim-owned logic: grep -c "moveStep\|takeDamage\|wallTarget" src/units/UnitView.ts returns 0
    - UnitView constructor takes an `id` param (grep: `id: string` in constructor signature)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no dangling Unit.ts importers broken)
  </acceptance_criteria>
  <done>UnitView is the Phaser render-half keyed by id with syncFrom + playDeathAnimation; sim-owned data/logic dropped; Unit.ts re-exported or removed cleanly; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Drive sim from GameScene.update — createWorld, step(), reconcile, event mapping</name>
  <files>src/scenes/GameScene.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (full battle loop: init :117-146, create :148-198, update :386-398, damageBase :546-563, triggerGameOver :585-605, recordResult :607-617, breakWall :779-791, drawWallOverlays :793-823)
    - src/sim/step.ts, src/sim/world.ts (step/createWorld/spawnUnit signatures from Plan 02)
    - src/units/UnitView.ts (Task 1 — the view to reconcile)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (sections "src/scenes/GameScene.ts" update render loop + reconcileUnits; "Reconcile pattern")
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Q1 extraction seam; Q3 events taxonomy + renderer actions; Risk 5 game_over idempotency; Risk 3 base_hit broadcast coupling)
  </read_first>
  <action>
    In `init()`/`create()`, build the sim world: call `createWorld({ hostSlot, guestSlot, mapBase: this.mapDef.base, mapOver: this.mapDef.over, isPractice: roomId starts with 'practice-', hostFaction, guestFaction })` using resolveSide(role, playerFaction) (Plan 01) for the factions, and store it as `this.world`. Remove the now-duplicated tower-build and wallHP-init from create() if createWorld owns them (the scene reads world.towers for TowerView). Add `this.unitViews = new Map<string, UnitView>()` and `this.pendingInputs: SimInput[] = []`. Rewrite `update(_t, dt)`: guard on `this.paused || this.world.over`; drain `const inputs = this.pendingInputs.splice(0)`; `const events = step(this.world, inputs, dt, Math.random)`; reconcile views by id (build live-id set from world.hostUnits+guestUnits, create UnitView+popIn for new ids, syncFrom existing, playDeathAnimation+delete for vanished ids); then loop events: `unit_died` → death handled by reconcile (no-op or trigger view death if still present); `wall_break` → drawWallOverlays + audio.playWallBreak + camera shake (180,0.009) + broadcast wall_break if non-practice; `base_hit` → flashBaseHit + broadcast base_hp if non-practice; `game_over` → triggerGameOver(ev.winner) ONCE (guard via world.over already set in sim; call recordResult once). Update the HUD each frame from world: goldEl=world.gold, timer from world.timeLeft, host/guestHPEl from world.hostBaseHp/guestBaseHp, updateSlotAffordability reads world.gold. Rewrite `tryDeployAt`: deduct world.gold (not this.gold/gameState.gold), push a `{type:'deploy', unitId, slot, role}` into pendingInputs (the sim spawns the unit on the next step), and broadcast deploy unchanged. Rewrite the channel handlers (:827-866): received `deploy` → push a deploy SimInput; received `wall_break` → push a wall_break SimInput (or apply to world directly, matching the current breakWall(...,false) no-rebroadcast path); received `base_hp` → directly set world.hostBaseHp/guestBaseHp and triggerGameOver if 0; received `game_over` → triggerGameOver(winner). Delete the five private updateX methods (updateGold/updateTimer/updateAI/updateUnits/updateTowers) and the old prune lines — step() owns them now. Keep damageBase/breakWall ONLY if still referenced by the received-broadcast paths; otherwise remove. Preserve the wire protocol byte-for-byte: same event names, same payload shapes.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - GameScene.update() calls `step(this.world, ...)` (grep: `step(this.world` present) and no longer calls updateGold/updateTimer/updateAI/updateUnits/updateTowers (grep -c "this.updateGold\|this.updateUnits\|this.updateTowers" returns 0)
    - The five private updateX methods are deleted (grep -c "private updateGold\|private updateUnits\|private updateTowers" src/scenes/GameScene.ts returns 0)
    - Reconcile is by id: grep `syncFrom` and a `Map<string, UnitView>` present; no reconcile-by-array-index
    - Wire protocol preserved: all four event names still present (grep -c "event: 'deploy'\|event: 'wall_break'\|event: 'base_hp'\|event: 'game_over'" >= 4)
    - game_over handled once: recordResult is gated by the game_over event / world.over (no double-call path)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 and `npx vitest run` exits 0 (all sim + Phase 9 tests green)
  </acceptance_criteria>
  <done>GameScene drives the sim via a single step() + id-reconcile + event mapping; five updateX methods deleted; wire protocol byte-preserved; sim is the source of truth for live battle state; tsc + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase broadcast | Opponent-supplied deploy/wall_break/base_hp/game_over payloads, cast with `as` (unchanged) |
| sim ↔ scene | sim emits events / consumes inputs; transport-free (D-04). Scene maps events↔broadcasts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | base_hp received → world overwrite | accept | Trust-and-overwrite preserved exactly as today (D-04, RESEARCH.md V5/Risk 3). Server validation is Phase 14. The extraction changes only the field owner (gameState→world), NOT the trust model — boundary is neither strengthened nor weakened. |
| T-10-03-02 | Spoofing | game_over / deploy broadcast | accept | Opponent can still broadcast events as in v1.0; this is the documented v1.0 trust debt Phase 14 closes. Phase 10 must NOT add or remove client-authority — it preserves the existing handling. |
| T-10-03-03 | Information disclosure | username in HUD innerHTML | accept | Flagged in CONCERNS.md; Phase 10 touches the HUD only for gold/timer/base-HP reads from world. Do NOT expand the innerHTML template (RESEARCH.md Security Domain). |
| T-10-03-PURE | Tampering | sim transport-freedom | mitigate | The sim still imports zero supabase; networking stays entirely in the scene. Verified by sim-file purity criteria carried from Plan 02. |

**Honest assessment:** Integration plan; preserves the existing wire protocol and trust boundaries byte-for-byte. No new external surface or I/O. The one deliberate change (state owner gameState→world) does not alter the trust model.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `npx vitest run` — all sim + Phase 9 tests green
- Sim purity unchanged: src/sim/*.ts still import zero phaser/supabase (regression check)
- Manual parity (deferred to Plan 05, D-16): two-session host+guest playtest confirms identical battle outcomes/feel; practice-mode AI battle plays identically
</verification>

<success_criteria>
- ROADMAP SC#1 complete: the battle loop is extracted into src/sim/ and runs the same battle with GameScene as a thin renderer
- D-02 (Unit split into SimUnit + UnitView), D-03 (reconcile + events), D-04 (transport-free sim, wire protocol preserved) all implemented
- Build tsc-green; no player-visible change pending the Plan 05 manual parity gate
</success_criteria>

<output>
Create `.planning/phases/10-services-simulation-refactor/10-03-SUMMARY.md` when done.
</output>
