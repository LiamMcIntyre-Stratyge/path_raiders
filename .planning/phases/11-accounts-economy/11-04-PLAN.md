---
phase: 11-accounts-economy
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - test/rls/inventory-rls.test.ts
  - test/rls/settlement-idempotency.test.ts
  - test/rls/migration.test.ts
autonomous: true
requirements: [ACCT-04, ECON-01, ECON-02, ECON-03, ECON-04, ECON-05]

must_haves:
  truths:
    - "Forged direct wallet/inventory writes leave the row unchanged — RLS denies (ECON-05)"
    - "spend_unlock deducts exactly the server cost, inserts one inventory row, returns insufficient_funds below cost, and is concurrent-safe (ECON-03/ECON-04)"
    - "report_match_result credits WIN_REWARD/LOSS_REWARD only on agreement, exactly once per match_id; mismatch voids, lone report pends; client cannot inject an amount (ECON-01/ECON-02/ECON-04, D-06/D-08)"
    - "v1.0 backfill provisions wallet + welcome grant + inventory from unlocked_units[] idempotently, preserving wins/losses (ACCT-04, D-09/D-10/D-11)"
    - "Full RLS suite is GREEN against the live-applied Plan 02 schema"
  artifacts:
    - path: "test/rls/inventory-rls.test.ts"
      provides: "GREEN: forged unlock, spend deduct, insufficient funds, concurrent spend"
    - path: "test/rls/settlement-idempotency.test.ts"
      provides: "GREEN: agreement settle, double-submit-once, already_settled, concurrent second-reporter, mismatch void, lone pending, no client amount"
    - path: "test/rls/migration.test.ts"
      provides: "GREEN: provision idempotency, existing-player migration, new-account provisioning"
  key_links:
    - from: "test/rls/settlement-idempotency.test.ts"
      to: "report_match_result + match_settlements"
      via: "two-user rpc + admin balance read-back"
      pattern: "report_match_result"
    - from: "test/rls/migration.test.ts"
      to: "provision_account + backfill"
      via: "admin seed profiles → provision → admin read wallet/inventory"
      pattern: "provision_account"
---

<objective>
Turn the Plan 01 RED RLS scaffolds GREEN against the live-applied Plan 02 schema. This plan
fully implements the idempotency / concurrency / forged-grant / mismatch-void / lone-report /
migration scenarios from VALIDATION.md — the "Looks Done But Isn't" gate for the economy.

Purpose: D-05/D-06/D-07 (interim trust posture) and ECON-04 (no double-spend, no double-grant,
never negative) are only real if proven by concurrent + replay tests. This plan is the proof.

Output: three GREEN RLS test files exercising every authority path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-accounts-economy/11-VALIDATION.md
@.planning/phases/11-accounts-economy/11-PATTERNS.md
@.planning/phases/11-accounts-economy/11-RESEARCH.md

<interfaces>
<!-- RPCs live (Plan 02 applied). Reward/grant/cost constants: -->
WIN_REWARD=50  LOSS_REWARD=15  WELCOME_GRANT=100  unit cost=100
spend_unlock(p_unit_id text) → {ok, new_balance, unit_id} | {ok:false, reason:'insufficient_funds'}
report_match_result(p_match_id uuid, p_claimed_winner uuid) → {status:'pending'|'settled'|'already_settled'|'void'}
provision_account(p_user_id uuid)

<!-- Harness (from wallet-rls.test.ts): admin=SERVICE+persistSession:false; user=ANON; createUser via admin.auth.admin.
     Keys from process.env only. vitest.config.ts has fileParallelism:false. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: inventory-rls.test.ts GREEN (ECON-03/04/05)</name>
  <files>test/rls/inventory-rls.test.ts</files>
  <read_first>
    - test/rls/wallet-rls.test.ts (harness + forged-write assertion shape to copy exactly)
    - test/rls/inventory-rls.test.ts (the Plan 01 RED scaffold being completed — keep its scenario list, fill in real assertions)
    - .planning/phases/11-accounts-economy/11-VALIDATION.md §Matrix rows: forged unlock, concurrent spend, spend below zero
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"Pitfall 5" (concurrent double-tap), §"Pitfall 6" (forged inventory insert)
  </read_first>
  <action>
    Complete inventory-rls.test.ts so all assertions are real against the live schema. One user, seeded balance
    100 via `credit_wallet`. Tests: (a) forged `from('inventory').insert({owner, unit_id:'assault_bot'})` →
    admin re-read shows 0 rows (ECON-05); (b) `spend_unlock('assault_bot')` → ok:true, new_balance 0, admin
    confirms exactly one inventory row + wallet balance 0 (ECON-03); (c) reseed to 50, `spend_unlock('assault_bot')`
    → ok:false reason 'insufficient_funds', balance unchanged 50 (ECON-03); (d) reseed to 100, `Promise.all` of
    two concurrent `spend_unlock('assault_bot')` → exactly one ok:true, balance ends at 0 (never negative), exactly
    one inventory row (ECON-04, Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run --project rls test/rls/inventory-rls.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project rls test/rls/inventory-rls.test.ts` exits 0 with the forged-unlock, deduct, insufficient-funds, and concurrent-spend tests all GREEN
    - The concurrent-spend test asserts final balance === 0 AND inventory row count === 1 (deducted at most once)
  </acceptance_criteria>
  <done>inventory RLS + spend authority fully proven: forged writes denied, atomic deduct, concurrent-safe.</done>
</task>

<task type="auto">
  <name>Task 2: settlement-idempotency.test.ts GREEN (ECON-01/02/04, D-06/D-08)</name>
  <files>test/rls/settlement-idempotency.test.ts</files>
  <read_first>
    - test/rls/wallet-rls.test.ts (single-user harness — extend to TWO users A,B per PATTERNS lines 597-614)
    - test/rls/settlement-idempotency.test.ts (Plan 01 RED scaffold being completed)
    - .planning/phases/11-accounts-economy/11-VALIDATION.md §Matrix rows: double-submit, re-submit after settled, concurrent second-reporters, mismatch void, lone report
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"Pitfall 1-4" (cross-user credit, GET DIAGNOSTICS, wrong-player credit, concurrent double-settle)
  </read_first>
  <action>
    Complete settlement-idempotency.test.ts with two users A and B (each its own ANON client). Per test use a fresh
    `match_id` uuid. Tests: (a) A reports winner=A, B reports winner=A → admin confirms A balance +50, B balance +15,
    one settled match_settlements row (ECON-01); (b) A reports winner=A twice (same match_id) before B → still pending,
    no credit; then B agrees → credited once, A balance +50 not +100 (ECON-04 double-submit); (c) after settled, A
    reports the same match again → status 'already_settled', balances unchanged (ECON-04); (d) concurrent
    `Promise.all([B.report(winner=A), A.report(winner=A)])` as the settling pair → exactly one settlement row, A
    credited exactly 50 (ECON-04, Pitfall 4); (e) mismatch: A reports winner=A, B reports winner=B → admin confirms
    match_settlements.voided=true, neither A nor B credited (D-06); (f) lone: only A reports → status 'pending', no
    settlement row, no credit (D-08); (g) ECON-02: confirm the RPC signature accepts only p_match_id + p_claimed_winner —
    passing an extra `p_amount` is ignored / errors and never changes the credited amount from the server constant 50/15.
  </action>
  <verify>
    <automated>npx vitest run --project rls test/rls/settlement-idempotency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project rls test/rls/settlement-idempotency.test.ts` exits 0
    - Double-submit and concurrent-second-reporter tests both assert winner credited === 50 exactly (not 100)
    - Mismatch test asserts `voided=true` and both balances unchanged; lone-report asserts `status:'pending'` and zero settlement rows
    - ECON-02 test asserts the credited amount equals the server constant regardless of any client-passed extra param
  </acceptance_criteria>
  <done>Both-agree settlement proven idempotent, mismatch-voiding, lone-pending, and server-amount-only.</done>
</task>

<task type="auto">
  <name>Task 3: migration.test.ts GREEN (ACCT-04)</name>
  <files>test/rls/migration.test.ts</files>
  <read_first>
    - test/rls/wallet-rls.test.ts (admin client pattern for seeding/reading rows with the SERVICE key)
    - test/rls/migration.test.ts (Plan 01 RED scaffold being completed)
    - .planning/phases/11-accounts-economy/11-VALIDATION.md §Matrix rows: migration idempotency, existing-player migration, new account provisioning
    - .planning/phases/11-accounts-economy/11-RESEARCH.md §"v1.0 Migration Mechanics" (backfill semantics) and §"Pitfall 7" (unknown unit_id tolerance)
  </read_first>
  <action>
    Complete migration.test.ts using the admin (SERVICE) client to seed and read. Tests: (a) admin creates a user +
    a profiles row with wins=5, losses=2, unlocked_units=['thorn_beast']; call `provision_account(user_id)`; assert
    wallet balance === 100 (WELCOME_GRANT), inventory contains 'thorn_beast', and profiles wins=5/losses=2/username
    unchanged (ACCT-04, D-09/D-11); (b) call `provision_account` again for the same user → wallet still 100 (welcome
    granted once), still one 'thorn_beast' inventory row (idempotency, D-02); (c) fresh user, profiles row with empty/null
    unlocked_units → provision → wallet 100, empty inventory (new-account provisioning, D-10 no back-pay); (d) optional:
    profile with unlocked_units containing an unknown id → provision does not crash (Pitfall 7).
    Note in a comment that the SQL DO-block backfill is the deploy-time equivalent of provision_account looped over
    all profiles; provision_account is the unit-testable surface of the same logic.
  </action>
  <verify>
    <automated>npx vitest run --project rls test/rls/migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project rls test/rls/migration.test.ts` exits 0
    - Existing-player test asserts wallet===100, inventory has 'thorn_beast', wins===5 & losses===2 unchanged
    - Idempotency test asserts a second provision_account leaves balance at 100 (not 200)
    - `npx vitest run --project rls` (the full RLS suite — all four files incl. existing wallet-rls) exits 0
  </acceptance_criteria>
  <done>v1.0 backfill proven: welcome grant once, earned units kept free, stats preserved, idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → RLS/RPC | Tests assert direct writes are denied and only RPCs mutate authoritative state |
| test env → SERVICE key | admin client uses SERVICE key from process.env only — never bundled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | double-grant / double-spend regressions | mitigate | concurrent + replay tests assert exactly-once credit/deduct; this plan is the regression gate for ECON-04 |
| T-11-12 | Spoofing | colluding-pair false winner | accept | mismatch-void test enforces disagreement = no payout; mutual-agreement collusion is the accepted interim risk (D-05), hardened in P14 |
| T-11-13 | Information Disclosure | SERVICE key leakage in test files | mitigate | keys from process.env only; harness copied from the audited wallet-rls.test.ts |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `npx vitest run --project rls` — all four RLS files GREEN (incl. existing wallet-rls.test.ts).
- Concurrent + double-submit tests assert exactly-once semantics.
- Mismatch-void and lone-report assert no payout.
</verification>

<success_criteria>
- inventory/settlement/migration RLS tests GREEN against the live Plan 02 schema.
- ECON-04 (idempotent, atomic, never-negative) proven by concurrent + replay scenarios.
- ACCT-04 v1.0 backfill proven (welcome once, units kept, stats preserved, idempotent).
</success_criteria>

<output>
Create `.planning/phases/11-accounts-economy/11-04-SUMMARY.md` when done.
</output>
