---
phase: 09-backend-foundations-integrity
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/types/index.ts
  - src/lib/gameState.ts
  - src/scenes/LobbyScene.ts
  - src/scenes/GameScene.ts
autonomous: true
requirements: [FND-02]
user_setup: []

must_haves:
  truths:
    - "No 'guest' identity literal remains in scenes or gameState (rooms role columns guest_id/guest_faction excepted)"
    - "gameState.userId is a required real UUID at every play entry point (no ?? 'guest' fallback)"
    - "Practice mode requires a signed-in session before it can start"
    - "Any scene reached without a session redirects to AuthScene rather than running unauthenticated"
  artifacts:
    - path: "src/scenes/LobbyScene.ts"
      provides: "Room create/join + practice gated on real userId (no 'guest' fallback)"
      contains: "gameState.userId"
    - path: "src/lib/gameState.ts"
      provides: "userId initialized null but treated as required UUID post-auth"
  key_links:
    - from: "src/scenes/LobbyScene.ts"
      to: "gameState.userId"
      via: "real-UUID precondition guard at play entry (no 'guest' fallback)"
      pattern: "if \\(!gameState\\.userId\\)"
---

<objective>
Implement email-only authenticated identity (FND-02, D-04/D-05/D-06): delete the collision-prone
`'guest'` IDENTITY literal outright, make `gameState.userId` a required real UUID precondition at
every play entry point (replacing the `gameState.userId ?? 'guest'` fallbacks at LobbyScene
:341/:425 and the unreachable guard at GameScene:607), and gate practice mode behind a real
session. Crucially, the rooms ROLE columns `guest_id`/`guest_faction` and the `role: 'guest'`
union STAY — only the identity literal `'guest'` is removed (Pitfall 7).

Purpose: An unauthenticated `'guest'` identity collides across players and is the Spoofing threat
this phase closes. After this, no scene is reachable without a session and every persistent write
is attributed to a real authenticated UUID.
Output: refactored LobbyScene/GameScene + gameState/types so `userId` is treated as required UUID.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-foundations-integrity/09-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Exact 'guest' IDENTITY-literal sites to remove (verified by grep this session): -->
- LobbyScene.ts:203  ternary text 'PLAYING AS GUEST' (UI string for unsigned state — now dead, becomes signed-in always)
- LobbyScene.ts:341  host_id: gameState.userId ?? 'guest'   → require real userId
- LobbyScene.ts:425  guest_id: gameState.userId ?? 'guest'  → require real userId
- LobbyScene.ts:310-319  practice onclick — gate on session before starting PlacementScene
- GameScene.ts:607  if (!gameState.userId) return  (in recordResult / now account.recordMatchResult — branch becomes unreachable, keep as defensive guard)

<!-- STAYS (role 'guest', NOT identity literal) — DO NOT remove: -->
- src/types/index.ts:27  role: 'host' | 'guest' | null   (game role union)
- rooms columns guest_id / guest_faction, gameState.role = 'guest', PlacementScene/GameScene role==='guest' checks

<!-- After plan 03, LobbyScene room writes go through rooms.createRoom/joinRoom; the host_id/guest_id
     values are now passed as args to those clients — remove the ?? 'guest' at the call site that builds the arg. -->

<!-- Session source of truth: AuthScene.checkSession() populates gameState.userId from session.user.id;
     after D-04/D-06 AuthScene is the sole entry (BootScene → AuthScene). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make userId a required-UUID precondition + remove 'guest' fallbacks in LobbyScene</name>
  <files>src/scenes/LobbyScene.ts</files>
  <read_first>
    - .planning/phases/09-backend-foundations-integrity/09-RESEARCH.md Pitfall 7 (delete identity literal, keep role 'guest'; gate at scene-entry not papering with 'guest')
    - src/scenes/LobbyScene.ts lines 200-227 (signed-in label + sign-out), 308-449 (practice + create/join, now via rooms.ts after plan 03)
  </read_first>
  <action>
    In LobbyScene: (1) Replace the `gameState.userId ?? 'guest'` expressions that build the
    `hostId`/`guestId` arguments passed to `rooms.createRoom`/`rooms.joinRoom` with a real-UUID
    precondition — add a guard at the top of each create/join/practice handler: `if
    (!gameState.userId) { this.scene.start('AuthScene'); return }`. After the guard, pass
    `gameState.userId` directly (TypeScript narrows it to `string`). (2) Replace the
    `'PLAYING AS GUEST'` ternary text (line 203) with the always-signed-in branch (the unsigned
    branch is now dead — render the signed-in label unconditionally, or keep a defensive redirect).
    (3) Gate the practice onclick (lines 310-319): add the same
    `if (!gameState.userId) { this.scene.start('AuthScene'); return }` before starting
    PlacementScene, so practice runs only after a real session (D-06). Do NOT touch `gameState.role
    = 'guest'`, `role: 'guest'` payloads, or the realtime channel block — those are roles, not the
    identity literal.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -nE "\\?\\? 'guest'" src/scenes/LobbyScene.ts && ! grep -q "PLAYING AS GUEST" src/scenes/LobbyScene.ts && grep -q "if (!gameState.userId)" src/scenes/LobbyScene.ts && grep -q "role: 'guest'" src/scenes/LobbyScene.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "?? 'guest'" src/scenes/LobbyScene.ts` returns no matches
    - `grep "PLAYING AS GUEST" src/scenes/LobbyScene.ts` returns no matches
    - A `if (!gameState.userId) ... AuthScene` guard precedes create, join, and practice handlers
    - `role: 'guest'` payload (the ROLE) still present — not removed
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>LobbyScene requires a real UUID at every play entry; the 'guest' identity literal is gone; roles preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm GameScene guard + sweep remaining identity-literal usage</name>
  <files>src/scenes/GameScene.ts</files>
  <read_first>
    - src/scenes/GameScene.ts lines 599-646 (recordResult guard — now account.recordMatchResult after plan 03)
    - .planning/phases/09-backend-foundations-integrity/09-RESEARCH.md Pitfall 7
  </read_first>
  <action>
    In GameScene, ensure the `if (!gameState.userId) return` guard at the head of the result-record
    path (line 607 region) stays as a defensive no-op (the branch is now unreachable since
    AuthScene is the sole entry, but keep it — do NOT replace it with a `'guest'` fallback). Verify
    no `'guest'` IDENTITY literal remains anywhere in GameScene (the many `'host' | 'guest'` ROLE
    occurrences and `side === 'guest'` comparisons STAY — they are roles). Do not alter battle
    logic. If `account.recordMatchResult` (plan 03) already encapsulates the guard, keep a thin
    scene-side `if (!gameState.userId) return` as well for clarity.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -nE "\\?\\? 'guest'|= 'guest'$|id: 'guest'|_id: 'guest'" src/scenes/GameScene.ts && grep -q "if (!gameState.userId) return" src/scenes/GameScene.ts</automated>
  </verify>
  <acceptance_criteria>
    - No identity-literal `'guest'` assignment remains in GameScene (role unions/comparisons excepted)
    - The `if (!gameState.userId) return` defensive guard is present
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>GameScene has no 'guest' identity fallback; defensive userId guard retained; roles untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Document required-UUID intent in types/gameState + final phase-wide identity sweep</name>
  <files>src/types/index.ts, src/lib/gameState.ts</files>
  <read_first>
    - src/types/index.ts (GameStateType.userId: string | null; role union)
    - src/lib/gameState.ts (initial userId: null)
    - .planning/codebase/CONVENTIONS.md (inline-comment doc style)
  </read_first>
  <action>
    Keep `gameState.userId` initialized to `null` (pre-auth) and `GameStateType.userId: string |
    null`, but add an inline comment on the type field documenting the invariant: `userId: string |
    null  // null only before sign-in; required real UUID at every play entry (FND-02, D-05)`. Do
    NOT widen or add a `'guest'` member. Leave the `role: 'host' | 'guest' | null` union exactly as
    is. Then run a phase-wide sweep to confirm no IDENTITY-literal `'guest'` remains in
    `src/scenes/` or `src/lib/gameState.ts` (excluding the role union and `_id`/role usages).
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -rnE "\\?\\? 'guest'" src/scenes/ src/lib/gameState.ts && grep -q "role: 'host' | 'guest' | null" src/types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "?? 'guest'" src/scenes/ src/lib/gameState.ts` returns no matches (identity literal gone phase-wide)
    - `src/types/index.ts` still declares `role: 'host' | 'guest' | null` (role preserved)
    - userId invariant documented via inline comment; no `'guest'` member added to userId's type
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The 'guest' identity literal is removed phase-wide; userId's required-UUID invariant is documented; roles preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated entry → game scenes | No scene may run without a verified session; identity must be a real UUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-guest-collision | Spoofing | shared `'guest'` identity literal used as host_id/guest_id/profiles key | mitigate | Delete the identity literal; require a real authenticated UUID at every play entry; redirect to AuthScene when `!gameState.userId` (FND-02) |
| T-09-unauth-path | Spoofing / Elevation | a code path reaching a scene without a session (null-deref or unattributed write) | mitigate | Scene-entry guards (`if (!gameState.userId) AuthScene`) make sign-in a hard precondition incl. practice (D-06); AuthScene remains sole entry |
| T-09-role-confusion | — | accidentally removing the ROLE `'guest'` (guest_id/guest_faction/role union) | accept/avoid | Explicitly preserved per Pitfall 7; grep gates assert `role: 'guest'` and `'host' | 'guest'` union remain |
</threat_model>

<verification>
- `grep -rn "?? 'guest'"` across `src/scenes/` and `src/lib/gameState.ts` returns no matches.
- `role: 'host' | 'guest' | null` union and rooms `guest_id`/`guest_faction` role usage remain intact.
- `npx tsc --noEmit` passes (userId narrows to string after the entry guards).
- The FND-02 static grep gate in CI (plan 05) passes.
</verification>

<success_criteria>
- The `'guest'` identity literal is deleted; `gameState.userId` is a required real UUID at play entry.
- Practice mode requires sign-in; no unauthenticated code path remains; game roles preserved.
</success_criteria>

<output>
Create `.planning/phases/09-backend-foundations-integrity/09-04-SUMMARY.md` when done.
</output>
