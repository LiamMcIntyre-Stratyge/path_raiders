---
phase: 10-services-simulation-refactor
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - src/types/index.ts
  - src/lib/gameState.ts
  - src/scenes/GameScene.ts
autonomous: true
requirements: [BATTLE-02]
must_haves:
  truths:
    - "gameState no longer carries live battle values (hostBaseHp, guestBaseHp, gold removed)"
    - "The dead gameMode field is removed from GameStateType"
    - "gameState is documented and treated as a session + read-through profile cache, not the mutated battle source"
    - "Persistent profile fields (userId, username, unlockedUnits, wins, losses) stay hydrated via the src/lib/api/account seam (write path unchanged)"
    - "The sim world is the sole source of truth for live battle state"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Slimmed GameStateType (battle fields removed)"
      contains: "interface GameStateType"
    - path: "src/lib/gameState.ts"
      provides: "Slimmed gameState singleton (session + profile cache)"
      contains: "const gameState"
  key_links:
    - from: "src/scenes/GameScene.ts"
      to: "src/sim/world.ts"
      via: "all live battle reads come from this.world, not gameState"
      pattern: "this.world.(gold|hostBaseHp|guestBaseHp)"
    - from: "src/scenes/GameScene.ts"
      to: "src/lib/api/account"
      via: "recordResult write path unchanged (D-13)"
      pattern: "recordMatchResult"
---

<objective>
Reduce `gameState` to a session/battle read-through cache (D-12/D-14): remove the live battle fields (`hostBaseHp`, `guestBaseHp`, `gold`) now owned by the sim world, remove the dead `gameMode` field, and document `gameState` as a cache rather than the mutated battle source. Keep the persistent profile fields (`userId`, `username`, `unlockedUnits`, `wins`, `losses`) hydrated via the Phase 9 `src/lib/api/account` seam with the write path unchanged (D-13 — authority moves are Phase 11/14, not here).

Per RESEARCH.md recommended sequence step 8: removal is compile-time enforcement — deleting these fields from `GameStateType` surfaces every remaining write site as a tsc error, which is the safety mechanism (Pitfall 1). This is a small, mechanical, tsc-driven plan that finalizes the gameState reduction once the sim (Plans 02-03) already owns the battle state.

Purpose: Makes the sim world the single source of truth for live battle state and leaves gameState as a clean session/profile cache — the shape Phase 11 builds on (it depends on the read-through gameState + recordResult-authority handoff).
Output: slimmed `GameStateType`, slimmed `gameState` singleton, and a GameScene with zero remaining `gameState.gold/hostBaseHp/guestBaseHp` references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-services-simulation-refactor/10-CONTEXT.md
@.planning/phases/10-services-simulation-refactor/10-RESEARCH.md
@.planning/phases/10-services-simulation-refactor/10-PATTERNS.md
@.planning/phases/10-services-simulation-refactor/10-03-SUMMARY.md

<interfaces>
<!-- Exact removal/keep lists — verified from src/types/index.ts and src/lib/gameState.ts. -->

GameStateType today (src/types/index.ts:18-35) — REMOVE these four (D-12/D-14):
  hostBaseHp: number      → now world.hostBaseHp
  guestBaseHp: number     → now world.guestBaseHp
  gold: number            → now world.gold
  gameMode: 'topdown' | 'portrait'  → never read in any scene [VERIFIED]

KEEP (session + profile cache, D-14):
  userId, username, playerFaction, unlockedUnits, loadout, wins, losses, roomId, role, mapId, hostSlot, guestSlot

gameState singleton (src/lib/gameState.ts:3-20) — drop the four matching initializers:
  hostBaseHp: 1000, guestBaseHp: 1000, gold: 200, gameMode: 'topdown'

Remaining gameState battle write/read sites to fix (Plans 02-03 should already have moved most; confirm none remain):
  - tryDeployAt deduction → world.gold (Plan 03)
  - updateGold gold write → world.gold (Plan 02 step.ts)
  - damageBase / base_hp handler → world.hostBaseHp/guestBaseHp (Plan 03)
  - init() this.gold = gameState.gold → world.gold via createWorld (Plan 03)

D-13 (UNCHANGED — do NOT touch): recordResult writes wins/losses/unlockedUnits via recordMatchResult
  (src/lib/api/account) after match end. Authority stays client-side this phase; Phase 11/14 owns the move.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove battle + dead fields from GameStateType and the gameState singleton</name>
  <files>src/types/index.ts, src/lib/gameState.ts</files>
  <read_first>
    - src/types/index.ts (lines 18-35 GameStateType — the four fields to remove)
    - src/lib/gameState.ts (full singleton init — drop the four matching initializers; add a cache doc-comment)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (sections "src/lib/gameState.ts" and "src/types/index.ts" — exact before/after)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Q5 gameState reduction table; Pitfall 1 remove-from-type-simultaneously)
  </read_first>
  <action>
    Edit `src/types/index.ts`: from `GameStateType` (:18-35) delete the four fields `hostBaseHp`, `guestBaseHp`, `gold`, and `gameMode`. Keep all session/profile fields (userId, username, playerFaction, unlockedUnits, loadout, wins, losses, roomId, role, mapId, hostSlot, guestSlot). Add a short doc-comment above the interface stating it is a session + read-through profile cache (D-14) — live battle state lives on SimWorld, persistent fields are hydrated via src/lib/api/account. Edit `src/lib/gameState.ts`: drop the `hostBaseHp: 1000`, `guestBaseHp: 1000`, `gold: 200`, `gameMode: 'topdown'` initializers from the singleton so it matches the slimmed type. This will surface tsc errors at every remaining battle write site — that is the intended compile-time enforcement; Task 2 fixes them. Do NOT change recordResult / api/account wiring (D-13).
  </action>
  <verify>
    <automated>grep -c "hostBaseHp\|guestBaseHp\|gameMode" src/lib/gameState.ts</automated>
  </verify>
  <acceptance_criteria>
    - GameStateType no longer declares hostBaseHp/guestBaseHp/gold/gameMode (grep -cE "hostBaseHp|guestBaseHp|gameMode|^\s*gold:" src/types/index.ts returns 0)
    - gameState singleton no longer initializes those four fields (grep -c "hostBaseHp\|guestBaseHp\|gameMode" src/lib/gameState.ts returns 0)
    - gameState.ts carries a doc-comment marking it a session/profile cache (grep -i "cache" src/lib/gameState.ts present)
    - Session + profile fields preserved (grep `userId`, `wins`, `unlockedUnits`, `roomId` all present in both files)
  </acceptance_criteria>
  <done>The four fields are gone from both the type and the singleton; gameState is documented as a cache; session/profile fields intact. NOTE: the build is INTENTIONALLY tsc-broken after this task — removing the fields is what surfaces the remaining battle write sites for Task 2 to fix. Do NOT commit after Task 1 standalone; commit only once Task 2 restores tsc-green (so no broken-build commit lands in git history). This task and Task 2 form a single atomic commit.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all surfaced battle write/read sites to use world; restore tsc-green</name>
  <files>src/scenes/GameScene.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (every site flagged by tsc after Task 1 — likely init :133, tryDeployAt :354-356, base_hp handler :852/:857, damageBase :550/:557 if still present)
    - src/sim/world.ts, src/sim/step.ts (world owns gold/hostBaseHp/guestBaseHp now)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Risk 4 gameState.gold two write sites; Q5 init handoff preservation)
  </read_first>
  <action>
    Run `npx tsc --noEmit -p tsconfig.json` and fix every error surfaced by the field removal. Replace each remaining `gameState.gold` read/write with `this.world.gold`, each `gameState.hostBaseHp` with `this.world.hostBaseHp`, each `gameState.guestBaseHp` with `this.world.guestBaseHp`. Remove any dead `this.gold = gameState.gold` assignment in init() (the world is initialized via createWorld with the gold default; if a carried-over gold value is needed across rematches, source it from world creation opts — but per current behavior init resets to the default, so no cross-scene gold persistence is required). Remove the now-unused private scalar fields on GameScene (this.gold, this.hostBaseHP, this.guestBaseHP) if Plan 03 left any — the world owns them. Confirm the HUD reads (gold/timer/base HP) all come from this.world (Plan 03 should have done this; verify no gameState battle reads remain). Do NOT alter recordResult or any api/account call (D-13). The GameScene.init handoff (roomId/role/playerFaction/mapId/slots into gameState) is preserved unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - No `gameState.gold`, `gameState.hostBaseHp`, or `gameState.guestBaseHp` references remain anywhere (grep -rc "gameState.gold\|gameState.hostBaseHp\|gameState.guestBaseHp" src/ returns 0)
    - No unused private `gold`/`hostBaseHP`/`guestBaseHP` scalar fields remain on GameScene (grep -c "private gold\|private hostBaseHP\|private guestBaseHP" src/scenes/GameScene.ts returns 0)
    - recordResult / recordMatchResult call path unchanged (grep `recordMatchResult` still present; D-13 write path intact)
    - GameScene.init still writes roomId/role/playerFaction/mapId/slots into gameState (handoff preserved)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 and `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Every battle field reference points at this.world; no gameState battle reads/writes remain; recordResult path unchanged; tsc + tests green. Commit Task 1 + Task 2 together here as a single atomic commit (the intermediate post-Task-1 broken state is never committed standalone).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase (recordResult) | Client-side match-result write via api/account — UNCHANGED this phase (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | client-side recordResult authority | accept | Deliberately preserved — D-13 keeps the v1.0 write path; moving result/economy writes server-side is Phase 11/14. This plan only relocates in-memory battle state (gameState→world); it adds NO new write authority and removes none. |
| T-10-04-02 | Information disclosure | gameState cache contents | accept | gameState holds only the player's own session/profile fields (read-through from api/account under Phase 9 RLS). Slimming it removes battle fields; it does not expose any new data. |

**Honest assessment:** Pure in-memory state relocation. No new external surface, no new I/O, no change to the recordResult trust boundary. The extraction does not weaken existing boundaries.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0 (the compile-error sweep is the safety net)
- `npx vitest run` — all sim + Phase 9 tests green
- grep confirms zero `gameState.{gold,hostBaseHp,guestBaseHp}` references in src/
- Manual parity (deferred to Plan 05, D-16): HUD shows correct gold/timer/base HP sourced from the sim; rematch ("Play Again") still initializes correctly
</verification>

<success_criteria>
- ROADMAP SC#3 complete: gameState reduced to a session/battle read-through cache; persistent fields read through src/lib/api/; battle state no longer mutated ad hoc on gameState
- D-12 (sim world is source of truth), D-13 (profile read-through cache, write path unchanged), D-14 (single slimmed gameState, documented as cache) all implemented
- Build tsc-green; no player-visible change pending the Plan 05 manual parity gate
</success_criteria>

<output>
Create `.planning/phases/10-services-simulation-refactor/10-04-SUMMARY.md` when done.
</output>
