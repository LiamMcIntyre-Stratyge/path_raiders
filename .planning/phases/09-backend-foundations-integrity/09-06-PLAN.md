---
phase: 09-backend-foundations-integrity
plan: 06
type: execute
wave: 5
depends_on: ["09-02", "09-05"]
files_modified: []
autonomous: false
requirements: [FND-01]
user_setup:
  - service: supabase
    why: "Push committed migrations to the LIVE Supabase project so the wallet table + RLS + credit RPC + tightened profiles RLS actually exist in prod (not just local CI)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (for non-interactive `supabase db push`)"
    dashboard_config:
      - task: "Provide the project ref for `supabase link --project-ref <ref>` (or run `supabase login` interactively)"
        location: "Supabase Dashboard → Project Settings → General (Reference ID)"

must_haves:
  truths:
    - "The committed migrations are applied to the LIVE Supabase project (wallet, RLS, credit RPC, profiles tighten, RLS shells all exist in prod)"
    - "The live profiles/rooms data is preserved (non-destructive push)"
    - "A signed-in client can read its own wallet and the credit RPC is callable in prod"
  artifacts: []
  key_links:
    - from: "supabase/migrations"
      to: "live Supabase project"
      via: "supabase db push (after supabase link)"
      pattern: "db push"
---

<objective>
[BLOCKING] Push the committed `supabase/migrations` (plan 02) to the LIVE Supabase project so the
wallet table + RLS + `credit_wallet` RPC + tightened `profiles` RLS + RLS shells actually exist in
production — not just in the local CI stack.

Purpose: build/tsc/local-RLS all pass WITHOUT the production push (CI uses a throwaway local
stack), creating a FALSE-POSITIVE verification state where the app's prod backend still lacks the
new schema. This plan closes that gap. It requires a project ref + access token the user controls,
so it is `autonomous: false` (manual intervention / user-supplied credentials).
Output: live Supabase project schema brought in sync with the committed migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-foundations-integrity/09-RESEARCH.md
@.planning/codebase/INTEGRATIONS.md

<interfaces>
<!-- Push contracts: -->
- `supabase link --project-ref <ref>` (needs SUPABASE_ACCESS_TOKEN env or `supabase login`)
- `supabase db push` applies un-applied committed migrations to the linked remote
- Baseline migration (plan 02) uses `create table if not exists` so it is non-destructive against existing live profiles/rooms (A3)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Link the live project and supply credentials</name>
  <read_first>
    - .planning/phases/09-backend-foundations-integrity/09-RESEARCH.md Assumption A3 (baseline non-destructive), §"Open Questions Q1"
    - .planning/codebase/INTEGRATIONS.md §"Environment Configuration" (Supabase project; anon key public)
  </read_first>
  <what-built>
    Plan 02 produced committed migrations validated against a local stack in CI (plan 05). They
    must now be applied to the live Supabase project. Claude cannot supply the project ref or an
    access token — only the user can.
  </what-built>
  <how-to-verify>
    1. Get the project Reference ID: Supabase Dashboard → Project Settings → General → Reference ID.
    2. Get an access token: Supabase Dashboard → Account → Access Tokens (create one) and export it
       non-interactively: `export SUPABASE_ACCESS_TOKEN=<token>` (or run `supabase login` for an
       interactive session).
    3. Run `supabase link --project-ref <ref>` from the repo root and confirm it links successfully.
    4. (Recommended) Run `supabase db push --dry-run` and confirm the listed migrations are exactly
       the baseline + foundations + profiles-RLS migrations from plan 02 (no destructive `drop`).
  </how-to-verify>
  <resume-signal>Type "linked" once `supabase link` succeeds and the dry-run shows only the expected non-destructive migrations, or paste any error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push migrations to the live project and verify</name>
  <read_first>
    - .planning/phases/09-backend-foundations-integrity/09-RESEARCH.md §"Code Examples → Wallet table + RLS + credit RPC" (what must exist post-push)
  </read_first>
  <what-built>
    With the project linked (Task 1), run `supabase db push` to apply the committed migrations to
    prod. Because the baseline uses `create table if not exists`, existing v1.0 `profiles`/`rooms`
    data is preserved; only the new wallet/ledger/shell tables, the credit RPC, and the profiles
    RLS policies are added.
  </what-built>
  <how-to-verify>
    1. Run `supabase db push` (with the linked project + SUPABASE_ACCESS_TOKEN). Confirm it reports
       the migrations applied with no errors and no destructive operations on profiles/rooms.
    2. In the Supabase Dashboard → Table Editor, confirm `wallet`, `wallet_credits`, `inventory`,
       `upgrades`, `match_results` now exist.
    3. Dashboard → Database → Functions: confirm `credit_wallet` exists and is `SECURITY DEFINER`.
    4. Dashboard → Authentication/Policies (or Table Editor → RLS): confirm RLS is enabled on
       `wallet` (SELECT-own, no client write policy) and on `profiles` (select/insert/update own).
    5. Confirm existing v1.0 profile rows are still present (no data loss).
  </how-to-verify>
  <verify>
    <human-check>Dashboard shows the new tables + credit_wallet (SECURITY DEFINER) + RLS enabled, with v1.0 profiles data intact.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db push` applied the committed migrations to the live project with no errors
    - `wallet`, `wallet_credits`, `inventory`, `upgrades`, `match_results`, and `credit_wallet` exist in prod
    - RLS is enabled on wallet (SELECT-own, no client write) and profiles (own-row policies)
    - Existing v1.0 profiles/rooms data is intact (non-destructive)
  </acceptance_criteria>
  <resume-signal>Type "pushed" once the dashboard confirms the schema + RLS + RPC exist and v1.0 data is intact, or paste any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migrations → live Postgres | Applying schema/RLS to prod must not strand or expose v1.0 data |
| access token → linked project | A privileged token authorizes the push; it stays in the user's shell/CI, never in src/ or the bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-prod-gap | Repudiation / Tampering | false-positive verification (local CI green, prod missing schema) | mitigate | This BLOCKING plan applies migrations to prod and verifies via dashboard before phase verification |
| T-09-dataloss | Tampering | push dropping/recreating live profiles/rooms | mitigate | Baseline uses `create table if not exists`; `db push --dry-run` reviewed for any `drop`; post-push row-presence check |
| T-09-token-leak | Elevation of Privilege | SUPABASE_ACCESS_TOKEN handling | mitigate | Token supplied via env/`supabase login` only; never committed, never a `VITE_*` var, never in src/ (bundle scan in plan 05 backstops) |
</threat_model>

<verification>
- `supabase db push` succeeds against the linked live project with no destructive ops.
- Dashboard confirms wallet/shells/credit_wallet/RLS exist and v1.0 data is intact.
- A signed-in client in the live app can read its own wallet row (empty/0) and the credit RPC is callable.
</verification>

<success_criteria>
- The committed migrations are live in the production Supabase project, with v1.0 data preserved.
- The false-positive verification gap (local-only schema) is closed before phase verification.
</success_criteria>

<output>
Create `.planning/phases/09-backend-foundations-integrity/09-06-SUMMARY.md` when done.
</output>
