---
phase: 12-progression-upgrades
plan: 03
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/sim/types.ts
  - src/sim/world.ts
  - src/sim/step.ts
autonomous: true
requirements: [PROG-03]
must_haves:
  truths:
    - "createWorld with hostTowerLevel=N builds host-side towers at resolveTowerStats(N) dmg; same for guest"
    - "spawnUnit resolves a deployed unit's hp/dmg from the world's per-side level map (resolveUnitStats), not the flat UNITS value"
    - "spawnAI (practice AI) resolves stats via resolveUnitStats from guestUnitLevels — empty map = level 1 (AI always base stats)"
    - "Omitting all level fields produces the exact pre-P12 battle (level-1 everywhere) — no behavior change for existing callers"
    - "src/sim/ imports zero Supabase/gameState (sim purity, P10 D-01)"
  artifacts:
    - path: "src/sim/types.ts"
      provides: "SimWorld level-map fields + SimInput deploy.level field"
      contains: "hostUnitLevels"
    - path: "src/sim/world.ts"
      provides: "CreateWorldOptions level fields; createWorld tower resolution; spawnUnit level resolution"
      contains: "resolveUnitStats"
    - path: "src/sim/step.ts"
      provides: "spawnAI stat resolution via resolveUnitStats (the landmine fix)"
      contains: "resolveUnitStats"
  key_links:
    - from: "src/sim/world.ts::createWorld"
      to: "src/towers/TowerData.ts::resolveTowerStats"
      via: "per-side tower level → tower.dmg/range/maxCd"
      pattern: "resolveTowerStats"
    - from: "src/sim/world.ts::spawnUnit"
      to: "src/units/UnitData.ts::resolveUnitStats"
      via: "world per-side unit level map → unit.hp/maxHp/dmg"
      pattern: "resolveUnitStats"
    - from: "src/sim/step.ts::spawnAI"
      to: "src/units/UnitData.ts::resolveUnitStats"
      via: "world.guestUnitLevels → AI unit hp/maxHp/dmg"
      pattern: "resolveUnitStats"
---

<objective>
Thread upgrade levels into the pure sim so both armies render at their persisted stats (PROG-03), without breaking sim purity or existing battle behavior.

Purpose: The sim runs on each client and spawns BOTH armies locally (D-11), so it must compute opponent stats from opponent levels. Per RESEARCH Focus Area 1, store the per-side level maps on `SimWorld` (set once in `createWorld` from `CreateWorldOptions`) so every spawn path — `spawnUnit` AND the `spawnAI` landmine in `step.ts` — resolves stats uniformly via the plan-01 resolvers. Levels are passed INTO the sim; the sim never imports Supabase/gameState (P10 D-01). Omitting all level fields reproduces the exact pre-P12 battle (level-1-invariant), so existing callers and tests need no change.

Output: Extended `SimWorld`/`CreateWorldOptions`/`SimInput`, level-resolving `createWorld`/`spawnUnit`, the `spawnAI` fix, and `test/unit/progression/sim-levels.test.ts` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-progression-upgrades/12-CONTEXT.md
@.planning/phases/12-progression-upgrades/12-PATTERNS.md
@.planning/phases/12-progression-upgrades/12-RESEARCH.md

<interfaces>
<!-- Resolver contracts from plan 12-01. Authoritative — call these, do not re-derive stats. -->

From src/units/UnitData.ts: resolveUnitStats(unitId, level): { hp, dmg }  // clamps level internally
From src/towers/TowerData.ts: resolveTowerStats(level): { dmg, range, maxCd }  // clamps level internally

Sim-purity allowlist (P10 D-01): src/sim/ may import only ../types, ../maps/MapData, ../towers/TowerData,
../units/UnitData, ../lib/pathfinder. NO Supabase, NO gameState, NO Phaser.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SimWorld + CreateWorldOptions + SimInput with level fields</name>
  <files>src/sim/types.ts, src/sim/world.ts</files>
  <read_first>
    - src/sim/types.ts (SimWorld struct, SimInput union — the in-file structs to extend; the sim-purity doc comment)
    - src/sim/world.ts (CreateWorldOptions interface ~lines 33-42; createWorld return object; spawnUnit ~lines 150-188)
    - .planning/phases/12-progression-upgrades/12-PATTERNS.md (sections "src/sim/types.ts" and "src/sim/world.ts" — exact field additions and where they go)
  </read_first>
  <action>
In `src/sim/types.ts`: add to `SimWorld` four fields — `hostUnitLevels: Record<string, number>`, `guestUnitLevels: Record<string, number>`, `hostTowerLevel: number`, `guestTowerLevel: number` (missing key / value defaults to level 1 per D-15). Extend the `SimInput` deploy variant to `{ type: 'deploy'; unitId: string; slot: number; role: 'host' | 'guest'; level?: number }` (optional `level` so existing callers compile unchanged; absent level falls back to the world map).

In `src/sim/world.ts`: add to `CreateWorldOptions` the four optional fields `hostTowerLevel?`, `guestTowerLevel?`, `hostUnitLevels?: Record<string, number>`, `guestUnitLevels?: Record<string, number>`. In the `createWorld` return object, set `hostUnitLevels: opts.hostUnitLevels ?? {}`, `guestUnitLevels: opts.guestUnitLevels ?? {}`, `hostTowerLevel: opts.hostTowerLevel ?? 1`, `guestTowerLevel: opts.guestTowerLevel ?? 1`. Do NOT change tower/unit stat assignment yet (that is Task 2) — this task only adds the carrying fields so the world holds the level maps.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -cE "hostUnitLevels|guestUnitLevels|hostTowerLevel|guestTowerLevel" src/sim/types.ts | awk '$1>=4'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SimWorld` declares the four level fields; `CreateWorldOptions` declares the four optional fields; `SimInput` deploy carries optional `level`; `createWorld` populates the four world fields with `?? {}` / `?? 1` defaults.
    - `npx tsc --noEmit -p tsconfig.json` clean (existing callers compile because all new fields are optional/defaulted).
  </acceptance_criteria>
  <done>Level-carrying fields added to SimWorld/CreateWorldOptions/SimInput; createWorld defaults them; prod tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve tower + unit + AI stats from levels (createWorld, spawnUnit, spawnAI) and turn sim-levels GREEN</name>
  <files>src/sim/world.ts, src/sim/step.ts</files>
  <read_first>
    - src/sim/world.ts (createWorld tower-build loop ~lines 65-93; spawnUnit hp/maxHp/dmg assignment ~lines 168-176)
    - src/sim/step.ts (spawnAI ~lines 17-45 — the landmine that constructs SimUnit inline with def.hp/def.dmg, bypassing spawnUnit)
    - src/units/UnitData.ts (resolveUnitStats — created in plan 01) and src/towers/TowerData.ts (resolveTowerStats)
    - .planning/phases/12-progression-upgrades/12-PATTERNS.md (sections "src/sim/world.ts" tower-build + spawnUnit changes, and "src/sim/step.ts" spawnAI replacement)
    - test/unit/progression/sim-levels.test.ts (the RED scaffold from plan 01 to turn GREEN)
  </read_first>
  <behavior>
    - createWorld({ hostTowerLevel: 3 }) → host-side towers have dmg === TOWER_LEVELS[2].dmg and > base TOWER_DMG; guest towers stay base.
    - spawnUnit at level 2 (via world.hostUnitLevels[unitId] = 2 or deploy.level = 2) → unit.hp === resolveUnitStats(id, 2).hp and unit.dmg === resolveUnitStats(id, 2).dmg; unit.maxHp === unit.hp.
    - spawnAI in practice (guestUnitLevels = {}) → AI unit stats equal resolveUnitStats(id, 1) (base) — no behavior change for practice.
    - createWorld with no level fields → all towers/units at level-1 stats (pre-P12 parity).
  </behavior>
  <action>
In `src/sim/world.ts` `createWorld`: import `resolveTowerStats` from `../towers/TowerData` and `resolveUnitStats` from `../units/UnitData`. Replace the tower-build loop's flat `TOWER_RANGE/TOWER_DMG/TOWER_CD` usage with per-side resolved stats — compute `const hostTowerStats = resolveTowerStats(opts.hostTowerLevel ?? 1)` and `const guestTowerStats = resolveTowerStats(opts.guestTowerLevel ?? 1)` and set each tower's `range/dmg/maxCd` from the side-appropriate stats (host-side towers ← hostTowerStats, guest-side ← guestTowerStats). Keep the existing flat constant imports for any other callers.

In `spawnUnit`: pick the side's level map `const levelMap = input.role === 'host' ? world.hostUnitLevels : world.guestUnitLevels`; resolve level `const unitLevel = input.level ?? levelMap[input.unitId] ?? 1` (explicit deploy.level wins, else world map, else 1); `const stats = resolveUnitStats(input.unitId, unitLevel)`; assign `hp: stats.hp, maxHp: stats.hp, dmg: stats.dmg`. Keep `speedPx: def.speedPx` and `attackRate: 900` flat (D-05 — speed/attack-rate do not scale).

In `src/sim/step.ts` `spawnAI`: import `resolveUnitStats` from `../units/UnitData`; before building the AI unit literal compute `const aiLevel = world.guestUnitLevels[def.id] ?? 1` and `const aiStats = resolveUnitStats(def.id, aiLevel)`; replace the literal's `hp: def.hp, maxHp: def.hp, dmg: def.dmg` with `hp: aiStats.hp, maxHp: aiStats.hp, dmg: aiStats.dmg`. (In practice mode `guestUnitLevels` is `{}` so AI always resolves to level 1 — intended.) No other change to `step.ts`; `step()` signature unchanged.

Then turn `test/unit/progression/sim-levels.test.ts` (RED from plan 01) GREEN by implementing against these fields — adjust the test's world-factory wrapper only if it referenced a field name that differs from the final shape; do NOT weaken its assertions.
  </action>
  <verify>
    <automated>npx vitest run --project unit -- sim-levels && npx vitest run --project unit -- sim</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run --project unit -- sim-levels` GREEN.
    - Regression: `npx vitest run --project unit -- sim` (existing movement/combat/characterization suites) still GREEN — pre-P12 battle behavior preserved when no levels supplied (level-1-invariant).
    - Source assertion: `world.ts` calls `resolveTowerStats` and `resolveUnitStats`; `step.ts` calls `resolveUnitStats` in spawnAI; sim purity holds — `grep -rE "supabase|gameState" src/sim/` returns nothing.
  </acceptance_criteria>
  <done>createWorld/spawnUnit/spawnAI resolve stats from levels; sim-levels GREEN; existing sim suites still GREEN; sim purity intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opponent level map → createWorld | Opponent levels arrive over the realtime channel (clamped in plan 04 before reaching here); the sim must not crash on extreme values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Denial of Service | resolver array index from a level field | mitigate | resolveUnitStats/resolveTowerStats clamp level internally (plan 01) — an out-of-range level cannot index out of bounds; clampLevels (plan 04) is the outer guard |
| T-12-09 | Tampering | opponent claims higher unit/tower level | accept (interim) | Clamp-only guard at the receive boundary (plan 04, D-12); server-side ownership-of-level verification deferred to P14 — accepted interim risk (mirrors Phase 11 posture) |
</threat_model>

<verification>
- `npx vitest run --project unit -- sim-levels` GREEN.
- `npx vitest run --project unit -- sim` (existing sim suites) GREEN — no battle behavior change at level 1.
- `npx tsc --noEmit -p tsconfig.json` clean.
- `grep -rE "supabase|gameState" src/sim/` empty (sim purity preserved).
</verification>

<success_criteria>
- Both participants' unit and tower battle stats derive from their levels (PROG-03), fed in via CreateWorldOptions/SimWorld.
- The spawnAI landmine is closed — AI resolves via the same resolver (level 1 in practice).
- Level-1-invariant: omitting levels reproduces the pre-P12 battle exactly.
- Sim remains Supabase/gameState-free (P10 D-01).
</success_criteria>

<output>
Create `.planning/phases/12-progression-upgrades/12-03-SUMMARY.md` when done.
</output>
