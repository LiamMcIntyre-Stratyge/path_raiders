---
phase: 10-services-simulation-refactor
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/sim/types.ts
  - src/sim/world.ts
  - src/sim/combat.ts
  - src/sim/step.ts
  - test/unit/sim/_helpers.ts
  - test/unit/sim/combat.test.ts
  - test/unit/sim/movement.test.ts
autonomous: true
requirements: [BATTLE-02]
must_haves:
  truths:
    - "A pure src/sim/ module exists with zero Phaser and zero Supabase imports"
    - "step(world, inputs, dt, rng) is the single tick entry point routing gold/timer/AI/units/towers"
    - "Combat resolution (nearest-target with D-07 id-tiebreak, cooldowns, takeDamage/death) runs in Node without Phaser"
    - "Unit movement along waypoints runs purely in the sim and is unit-tested"
    - "rng is injected (default Math.random); the only sim RNG is practice-AI spawning"
  artifacts:
    - path: "src/sim/types.ts"
      provides: "SimUnit, SimTower, SimWorld, SimEvent, SimInput type contracts"
      contains: "interface SimWorld"
    - path: "src/sim/world.ts"
      provides: "createWorld() factory + spawnUnit helper"
      exports: ["createWorld"]
    - path: "src/sim/combat.ts"
      provides: "processUnits, processTowers, takeDamage, moveUnit, nearest-target with tiebreak"
      exports: ["processUnits", "processTowers"]
    - path: "src/sim/step.ts"
      provides: "step(world, inputs, dt, rng) single tick entry point"
      exports: ["step"]
  key_links:
    - from: "src/sim/step.ts"
      to: "src/sim/combat.ts"
      via: "processUnits/processTowers calls in tick order"
      pattern: "processUnits\\("
    - from: "src/sim/combat.ts"
      to: "src/lib/pathfinder.ts"
      via: "findPath for recompute/detour"
      pattern: "findPath"
    - from: "test/unit/sim/combat.test.ts"
      to: "src/sim/step.ts"
      via: "step() driven from Vitest node env"
      pattern: "step\\(world"
---

<objective>
Build the pure, framework-agnostic `src/sim/` core (D-01): the `SimUnit`/`SimTower`/`SimWorld`/`SimEvent`/`SimInput` type contracts, a `createWorld()` factory, the combat/movement resolution (`combat.ts`), and the single `step(world, inputs, dt, rng)` tick entry point (D-08). Apply the D-07 id-tiebreak to both nearest-target sorts and inject `rng` (D-06). This is the keystone of the phase — it lifts the battle physics out of `GameScene` into plain TypeScript that runs in Vitest's node environment with zero Phaser and zero Supabase imports.

Per RESEARCH.md recommended sequence (steps 3-5): define types + createWorld first (tsc compiles), then extract combat.ts **test-first** (combat + movement tests are this plan's Wave 0 scaffold), then assemble step.ts. The sim is NOT yet wired into GameScene (that is Plan 03) — GameScene still runs its own update() this plan, so the build stays green and behavior unchanged.

Purpose: Creates the testable, Phase-14-ready simulation seam. The pure shape (injected rng, single step entry, variable dt preserved) lets Phase 14 drop in a seeded mulberry32 + fixed-timestep accumulator at the same call sites without re-architecting.
Output: `src/sim/{types,world,combat,step}.ts` + the combat and movement test files (D-17 a/b) under `test/unit/sim/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-services-simulation-refactor/10-CONTEXT.md
@.planning/phases/10-services-simulation-refactor/10-RESEARCH.md
@.planning/phases/10-services-simulation-refactor/10-PATTERNS.md

<interfaces>
<!-- Exact field sets and signatures — executor uses these directly, no exploration needed. -->

SimUnit field set [VERIFIED from src/units/Unit.ts]:
  id: string (monotonic world.nextId++); defId: string (key into UNITS); faction: string;
  x: number; y: number; hp: number; maxHp: number; dir: 1 | -1; laneSlot: number (0|1|2);
  attackCd: number; attackRate: number (default 900); speedPx: number; dmg: number;
  waypoints: {x:number;y:number}[]; wpIdx: number; wallTarget: [number,number]|null; dead: boolean

SimTower field set:
  cx: number; cy: number; slotIdx: number; isHostSide: boolean;
  range: number; dmg: number; cd: number; maxCd: number

SimWorld field set [VERIFIED from GameScene fields :74-113 + create() :157-180]:
  gold; goldAccum; timeLeft; hostBaseHp; guestBaseHp; hostUnits: SimUnit[]; guestUnits: SimUnit[];
  towers: SimTower[]; mutableOver: OverlayType[][]; wallHP: Map<string,number>; over: boolean;
  isPractice; aiTimer; aiInterval; hostFaction; guestFaction; hostSlot; guestSlot;
  mapBase: TerrainType[][]; nextId; tickCount

SimEvent union:
  {type:'unit_died';unitId;x;y;faction} | {type:'wall_break';row;col} |
  {type:'base_hit';side:'host'|'guest';newHp} | {type:'game_over';winner:'host'|'guest'|'tie'}

SimInput union:
  {type:'deploy';unitId;slot;role:'host'|'guest'} | {type:'wall_break';row;col}

step signature: step(world: SimWorld, inputs: SimInput[], dt: number, rng: () => number = Math.random): SimEvent[]

Constants to reuse (do NOT redefine):
  COMBAT_RANGE = 52, BASE_REACH_DMG = 60 (from src/units/Unit.ts — re-export or move to sim)
  WALL_MAX_HP { wall:250, break_mach:200, break_plant:200, break_wiz:200 }; WALL_OVERLAYS set (GameScene :19-22)
  Gold tick: +10 every 2000ms accum, cap 9999 (GameScene updateGold :400-408)
  Timer: timeLeft -= dt/1000, start 180s (GameScene updateTimer :411-423)
  AI: aiInterval 6000ms; rng call 1 = oppPool[floor(rng()*len)], rng call 2 = floor(rng()*3) slot (GameScene updateAI :434/:437)

From src/lib/pathfinder.ts (pure, Phase 9 tested — call directly):
  findPath(startR, startC, goals: Cell[], base, over, faction): Cell[]
  canBreakWall(ov: OverlayType, faction: string): boolean

From src/maps/MapData.ts: CELL=36, COLS=22, ROWS=16, BASE_SLOTS, HOST_ROWS=[14,15], GUEST_ROWS=[0,1],
  slotWorldX(slot), hostSpawnY(), guestSpawnY()

From src/towers/TowerData.ts (Plan 01): TOWER_RANGE, TOWER_DMG, TOWER_CD, TOWER_DEF
From src/lib/sideHelper.ts (Plan 01): resolveSide(role, playerFaction), opponentFaction(faction)

Test scaffold analog — test/unit/pathfinder.test.ts makeBase/makeOver helpers (replicate in _helpers.ts):
```typescript
const ROWS = 16, COLS = 22
function makeBase(fill: TerrainType = 'open'): TerrainType[][] { ... }
function makeOver(fill: OverlayType = null): OverlayType[][] { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define src/sim/types.ts + src/sim/world.ts (createWorld factory + spawnUnit)</name>
  <files>src/sim/types.ts, src/sim/world.ts</files>
  <read_first>
    - src/units/Unit.ts (full — the field set SimUnit lifts; COMBAT_RANGE/BASE_REACH_DMG exports)
    - src/lib/gameState.ts (singleton-init analog for createWorld factory shape)
    - src/scenes/GameScene.ts (lines 19-22 WALL_MAX_HP/WALL_OVERLAYS; lines 117-180 init/create — wallHP init + tower build; lines 337-369 tryDeployAt — the spawn geometry spawnUnit mirrors)
    - src/maps/MapData.ts (CELL/ROWS/COLS/BASE_SLOTS/slotWorldX/hostSpawnY/guestSpawnY)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (sections "src/sim/types.ts" and "src/sim/world.ts" — full field sets + createWorld body)
  </read_first>
  <action>
    Create `src/sim/types.ts` with pure type declarations only (the sole runtime-free import allowed is `import type { OverlayType, TerrainType } from '../types'`; NO phaser, NO supabase). Declare `SimUnit`, `SimTower`, `SimWorld`, `SimEvent`, `SimInput` using the exact field sets in the interfaces block above. Create `src/sim/world.ts` exporting a `createWorld(opts)` factory (NOT a singleton — tests instantiate fresh per scenario) that mirrors GameScene.create() :157-180: build `mutableOver` from opts.mapOver, populate `wallHP` from WALL_OVERLAYS using WALL_MAX_HP, build the 6 towers (3 slots × 2 sides, isHostSide true at cy=13.5*CELL and false at cy=1.5*CELL, range/dmg/cd from TowerData), and initialize gold (default 200), goldAccum 0, timeLeft 180, host/guestBaseHp 1000, empty unit arrays, over false, isPractice/aiTimer 0/aiInterval 6000, hostFaction/guestFaction/hostSlot/guestSlot from opts, nextId 1, tickCount 0. Also export a `spawnUnit(world, input: {unitId, slot, role}, events)` helper that mirrors tryDeployAt's spawn+path geometry (:359-369): resolve dir (host=-1, guest=+1), spawnX=slotWorldX(slot), spawnY=host?hostSpawnY():guestSpawnY(), look up the UnitDefinition from UNITS by unitId, build a SimUnit (id=`String(world.nextId++)`, copy hp/maxHp/dmg/speedPx/attackRate=900/faction from def, attackCd 0, wallTarget null, dead false), compute initial waypoints via findPath (the assignPath logic at :749-760: target slot/rows by dir, map cells to {x,y} pixel centers), and push to world.hostUnits/guestUnits. Re-export or relocate COMBAT_RANGE/BASE_REACH_DMG so the sim owns them without importing Unit.ts (which imports Phaser). Do NOT wire any of this into GameScene yet.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx tsc --noEmit -p tsconfig.test.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/sim/types.ts` exports `SimUnit`, `SimTower`, `SimWorld`, `SimEvent`, `SimInput` (grep all five present)
    - `src/sim/world.ts` exports `createWorld` and `spawnUnit` (grep both present)
    - Sim purity: no phaser/supabase import in either file (grep -v '^//' src/sim/types.ts src/sim/world.ts | grep -cE "from 'phaser'|from '@supabase|lib/supabase|units/Unit'" returns 0)
    - SimUnit has all 18 fields from the verified set incl. `wallTarget: [number, number] | null` and `dead: boolean` (grep both)
    - Both `npx tsc --noEmit -p tsconfig.json` and `-p tsconfig.test.json` exit 0
  </acceptance_criteria>
  <done>Pure sim types + createWorld factory + spawnUnit compile clean under both prod and test tsconfig; zero Phaser/Supabase; field sets match the verified contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract combat.ts test-first (processUnits/processTowers, D-07 tiebreak, movement)</name>
  <files>src/sim/combat.ts, test/unit/sim/_helpers.ts, test/unit/sim/combat.test.ts, test/unit/sim/movement.test.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (lines 453-543: updateUnits + updateTowers — the exact extraction source; lines 763-791 recomputeUnitPath/damageWall/breakWall)
    - src/units/Unit.ts (lines 66-94: moveStep, isAtGoal, takeDamage — pure math to move into sim)
    - test/unit/pathfinder.test.ts (makeBase/makeOver/paintOver scaffold to replicate in _helpers.ts)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (section "src/sim/combat.ts" — full annotated source + D-07 tiebreak; Anti-Pattern Index)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Q8 landmines: processing-order host-then-guest, wallTarget guard priority, unit.active→!u.dead, prune-after-both-passes)
  </read_first>
  <behavior>
    - Test (movement, D-17a): a unit with one waypoint advances toward it each step(); speedPx*dt/1000 per step; arriving increments wpIdx
    - Test (combat, D-17b): two enemy units within COMBAT_RANGE (52px) attack on attackCd cooldown; lower-HP unit reaches hp<=0 first and emits a `unit_died` event with its id; dead=true is set
    - Test (D-07 tiebreak): two enemies at exactly equal distance → the lower string-id is targeted deterministically across repeated runs
    - Test (wallTarget priority guard): a unit with wallTarget set attacks the wall and does NOT also run a combat scan or movement that frame
  </behavior>
  <action>
    First write `test/unit/sim/_helpers.ts` replicating the pathfinder test's makeBase/makeOver/paintOver grid helpers plus a tiny `spawnTestUnit(world, opts)` convenience. Then write `test/unit/sim/movement.test.ts` (D-17a) and `test/unit/sim/combat.test.ts` (D-17b + the tiebreak + wallTarget-guard cases) per the behavior block — these MUST fail first (RED) because combat.ts does not exist. Then implement `src/sim/combat.ts` exporting `processUnits(world, events, dt)` and `processTowers(world, events, dt)`, plus pure helpers `moveUnit(u, dt)`, `isAtGoal(u)`, `takeDamage(u, amount, events)`. Extract updateUnits (:453-523) faithfully: the inner `processUnits(movers, enemies)` closure runs both `(hostUnits, guestUnits)` THEN `(guestUnits, hostUnits)` — pruning happens in step.ts AFTER both, NOT inside combat (landmine: a unit killed early is still present for the second pass). Preserve the three-priority order exactly: (1) wallTarget attack — if set, damage the wall via a sim damageWall that mutates wallHP and pushes `wall_break` when hp<=0, then `continue` (do NOT also scan combat/move); (2) combat — filter enemies within COMBAT_RANGE, sort by distance WITH the D-07 tiebreak `da - db || (a.id < b.id ? -1 : 1)`, attack the nearest on cooldown, takeDamage pushes `unit_died` if the target dies; (3) movement — if isAtGoal with no waypoints recompute via findPath; if at goal with waypoints exhausted reaching base, push `base_hit` (decrement world.hostBaseHp/guestBaseHp, push `game_over` if 0) and self-takeDamage(9999); else wall-check next waypoint (set wallTarget if canBreakWall) or moveUnit. Replace EVERY `unit.active` check with `!u.dead` (landmine 6/Pitfall 3). Extract updateTowers (:526-543) into processTowers: decrement cd, skip if cd>0, target the opposite side's units, filter in-range, sort with the SAME D-07 tiebreak, takeDamage nearest, reset cd to maxCd. No Phaser, no Supabase, no audio imports anywhere in combat.ts (audio is the scene's job — sim only emits events/continuous state).
  </action>
  <verify>
    <automated>npx vitest run test/unit/sim/movement.test.ts test/unit/sim/combat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - movement.test.ts and combat.test.ts both pass (vitest exits 0)
    - combat.ts exports `processUnits` and `processTowers` (grep both)
    - D-07 tiebreak present in BOTH sorts: grep -c "a.id < b.id" src/sim/combat.ts returns 2
    - Sim purity: grep -v '^//' src/sim/combat.ts | grep -cE "from 'phaser'|supabase|lib/audio|units/Unit'" returns 0
    - No `.active` Phaser check remains in combat.ts (grep -c "\.active" src/sim/combat.ts returns 0)
    - combat test asserts the lower-id target is chosen for equal-distance enemies (tiebreak determinism case present)
    - `npx tsc --noEmit -p tsconfig.test.json` exits 0
  </acceptance_criteria>
  <done>combat.ts extracted test-first; movement + combat + tiebreak + wallTarget-guard tests green; processing order and prune-after-both preserved; .active→!u.dead; zero Phaser/Supabase/audio; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble src/sim/step.ts (single tick entry, gold/timer/AI, rng injection, prune-after)</name>
  <files>src/sim/step.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (lines 386-451: update() + updateGold + updateTimer + updateAI — the five-method seam step() replaces; lines 395-397 prune)
    - src/sim/combat.ts (Task 2 — processUnits/processTowers signatures)
    - src/sim/world.ts (Task 1 — spawnUnit signature for the deploy input)
    - src/lib/sideHelper.ts (Plan 01 — opponentFaction for AI opponent pool; world.guestFaction is preset so no gameState import needed, Pitfall 5)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (section "src/sim/step.ts" — full step() body)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Pattern 3 step structure; D-06 rng injection; D-08 single entry / variable dt preserved)
  </read_first>
  <action>
    Create `src/sim/step.ts` exporting `step(world, inputs, dt, rng = Math.random): SimEvent[]` exactly mirroring GameScene.update's call order (:386-398). Body in order: (1) apply inputs — for each `deploy` input call spawnUnit(world, input, events); `wall_break` inputs mutate world (sim applies the opponent's wall break). (2) Gold: world.goldAccum += dt; while >= 2000 subtract 2000 and world.gold = min(gold+10, 9999) — strip ALL DOM/gameState writes that updateGold did. (3) Timer: world.timeLeft = max(0, timeLeft - dt/1000); if <=0 and !over compute winner by base-HP comparison, set over=true, push `game_over`. (4) AI spawn (only if world.isPractice && !world.over): aiTimer += dt; when >= aiInterval reset and call a `spawnAI(world, rng, events)` that uses the TWO injected-rng calls — `oppPool[Math.floor(rng()*oppPool.length)]` for unit type and `Math.floor(rng()*3)` for slot — reading the opponent pool from world.guestFaction (preset at createWorld via resolveSide; do NOT import gameState). Behavior with default Math.random is identical to today. (5) processUnits(world, events, dt) if !over. (6) processTowers(world, events, dt) if !over. (7) Prune: world.hostUnits = filter(!u.dead), world.guestUnits = filter(!u.dead) — AFTER both combat passes (landmine). Increment world.tickCount. Return events. Keep variable dt (D-08) — no fixed timestep, no seeded play activated. Still NOT wired into GameScene this plan.
  </action>
  <verify>
    <automated>npx vitest run test/unit/sim && npx tsc --noEmit -p tsconfig.test.json</automated>
  </verify>
  <acceptance_criteria>
    - step.ts exports `step` with signature `step(world, inputs, dt, rng = Math.random)` (grep: `rng` default `Math.random` present)
    - Tick order matches GameScene.update: inputs → gold → timer → AI → units → towers → prune (verifiable by reading; processUnits called before processTowers, prune after both)
    - Exactly the two AI rng calls exist (grep -c "rng()" src/sim/step.ts >= 2) and combat has none
    - world.tickCount incremented each call (grep `tickCount` present)
    - Sim purity: grep -v '^//' src/sim/step.ts | grep -cE "from 'phaser'|supabase|gameState|lib/audio" returns 0
    - All existing sim tests still green: `npx vitest run test/unit/sim` exits 0
    - `npx tsc --noEmit -p tsconfig.test.json` exits 0
  </acceptance_criteria>
  <done>step() is the single tick entry routing gold/timer/AI/units/towers with injected rng and prune-after-both; variable dt preserved; pure; tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase broadcast | Opponent-supplied deploy/wall/base_hp/game_over payloads (handled in scene, not sim) |
| sim ↔ scene | The sim consumes plain `inputs` and emits `events`; it is transport-free (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-PURE | Tampering | src/sim/ trust boundary | mitigate | Sim purity is the security control: zero Supabase/Phaser imports means the sim cannot be a network/render attack surface. Enforced by vitest node env (Phaser import throws) + grep acceptance criteria on every sim file. |
| T-10-02-01 | Tampering | Existing base_hp trust-and-overwrite | accept | Out of scope — preserved byte-for-byte (D-04); server validation is Phase 14. The extraction must NOT weaken this boundary: sim stays transport-free, scene keeps owning networking, no client-authority added/removed. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit intentionally empty — pure refactor, zero new dependencies). Nothing to slopcheck. |

**Honest assessment:** Behavior-preserving internal refactor. No new external surface, no new I/O. Sim purity strengthens (does not weaken) the existing boundary by removing transport from the battle logic.
</threat_model>

<verification>
- `npx vitest run test/unit/sim` — movement + combat tests green (D-17 a/b coverage starts here)
- `npx tsc --noEmit -p tsconfig.json` and `-p tsconfig.test.json` both exit 0
- `npx vitest run` — all Phase 9 + new sim tests green
- Sim purity is grep-enforced on every src/sim/*.ts file (no phaser/supabase/audio/gameState imports)
- GameScene still runs its own update() this plan → no player-visible change yet (wiring is Plan 03)
</verification>

<success_criteria>
- ROADMAP SC#1 (partial): the battle loop physics now lives in a standalone src/sim/ module (wiring completes in Plan 03)
- ROADMAP SC#2 (partial): core combat + movement unit tests green in the Phase 9 harness (win + wall-break + snapshot complete in Plan 05)
- D-01 (pure module), D-06 (rng injected), D-07 (id-tiebreak on both sorts — the one accepted micro behavior-change), D-08 (single step entry, variable dt) all implemented
- Build tsc-green at every commit
</success_criteria>

<output>
Create `.planning/phases/10-services-simulation-refactor/10-02-SUMMARY.md` when done.
</output>
