---
phase: 10-services-simulation-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/towers/TowerData.ts
  - src/towers/TowerView.ts
  - src/lib/sideHelper.ts
  - src/scenes/GameScene.ts
autonomous: true
requirements: [BATTLE-02]
must_haves:
  truths:
    - "Tower stats (range 6*CELL, dmg 25, cd 1400ms) come from a dedicated src/towers/ module, not inline GameScene constants"
    - "Tower rendering is a dedicated TowerView module mirroring the UnitView split"
    - "Side/faction resolution is a single pure resolveSide() helper, not duplicated in 3 GameScene methods"
    - "Battle plays identically — towers fire, draw, and target exactly as before"
  artifacts:
    - path: "src/towers/TowerData.ts"
      provides: "Flat static tower stat table (TOWER_RANGE, TOWER_DMG, TOWER_CD)"
      contains: "TOWER_RANGE"
    - path: "src/towers/TowerView.ts"
      provides: "Phaser tower rendering extracted from GameScene.drawTowers"
      contains: "drawTowers"
    - path: "src/lib/sideHelper.ts"
      provides: "Pure resolveSide(role, playerFaction) + opponentFaction(faction)"
      exports: ["resolveSide", "opponentFaction"]
  key_links:
    - from: "src/scenes/GameScene.ts"
      to: "src/towers/TowerData.ts"
      via: "import TOWER_RANGE/TOWER_DMG/TOWER_CD"
      pattern: "from '../towers/TowerData'"
    - from: "src/scenes/GameScene.ts"
      to: "src/lib/sideHelper.ts"
      via: "resolveSide in drawBasePlacements/drawTowers/updateAI"
      pattern: "resolveSide\\("
---

<objective>
Promote towers out of the inline `GameScene` definition into a dedicated `src/towers/` module (D-09/D-10), and centralize the side/faction-resolution helper duplicated in three GameScene methods into one pure function (D-11). This is the lowest-risk first step of the Phase 10 extraction (per RESEARCH.md recommended sequence step 1-2): towers are the simplest entity (stationary, no pathfinding) and `resolveSide` is a pure refactor. Both keep the build tsc-green and behavior byte-identical.

Purpose: Fills the empty `src/towers/` directory consistent with the Unit abstraction's data/view split, and removes the duplicated-faction-mapping divergence risk (CONCERNS) before the heavier sim extraction touches the same code.
Output: `src/towers/TowerData.ts`, `src/towers/TowerView.ts`, `src/lib/sideHelper.ts`, plus a thinned `GameScene` that imports them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-services-simulation-refactor/10-CONTEXT.md
@.planning/phases/10-services-simulation-refactor/10-RESEARCH.md
@.planning/phases/10-services-simulation-refactor/10-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/units/UnitData.ts (the structural template TowerData mirrors):
```typescript
import type { UnitDefinition } from '../types'
export const UNITS: UnitDefinition[] = [ /* flat static table */ ]
```

From src/maps/MapData.ts (tower geometry depends on these):
```typescript
export const CELL = 36
export function slotWorldX(slot: number): number  // BASE_SLOTS[slot].centerCol * CELL
```

From src/types/index.ts:
```typescript
export type Faction = 'machines' | 'plants' | 'wizards'
```

GameScene inline tower constants today (GameScene.ts:168-180, to centralize):
  TOWER_RANGE = 6 * CELL (216px), TOWER_DMG = 25, TOWER_CD = 1400 (ms)
  6 towers built in create(): 3 slots × 2 sides; isHostSide:true → hostTowerY = 13.5*CELL (attacks guestUnits);
  isHostSide:false → guestTowerY = 1.5*CELL (attacks hostUnits).

GameScene FC color lookup (GameScene.ts:51-56, used by drawTowers):
```typescript
const FC = { machines:{fill:0x1a5090,lite:0x3a80c0,bd:0x70b0ff}, plants:{...}, wizards:{...} }
const fac = (f: string) => FC[f] ?? FC.machines
```

The three duplicated side-resolution blocks (GameScene.ts:244-248, :297-301, :319-323):
```typescript
const role = gameState.role ?? 'host'
const pFac = gameState.playerFaction ?? 'machines'
const oppFac = this.opponentFaction(pFac)   // machines→plants→wizards→machines
const hostFac  = role === 'guest' ? oppFac : pFac
const guestFac = role === 'guest' ? pFac   : oppFac
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract resolveSide + opponentFaction into src/lib/sideHelper.ts</name>
  <files>src/lib/sideHelper.ts, src/scenes/GameScene.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (lines 239-323: drawBasePlacements, drawTowers, opponentFaction — the three duplication sites)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (section "D-11: resolveSide helper" — exact target code)
    - .planning/phases/10-services-simulation-refactor/10-RESEARCH.md (Pitfall 5: AI spawner faction resolution must NOT import gameState)
  </read_first>
  <action>
    Create `src/lib/sideHelper.ts` exporting two pure functions with zero Phaser/Supabase/gameState imports (imports only the `Faction` type from `../types`). `opponentFaction(f: Faction): Faction` returns the machines→plants→wizards→machines cycle (machines→plants, plants→wizards, else→machines), lifting the logic from GameScene.opponentFaction at :319-323. `resolveSide(role: 'host'|'guest', playerFaction: Faction): { hostFaction: Faction; guestFaction: Faction; dir: 1 | -1 }` returns hostFaction = role==='guest' ? opp : playerFaction, guestFaction = role==='guest' ? playerFaction : opp, and dir = role==='host' ? -1 : 1 (host moves up). Then update the three GameScene call sites (drawBasePlacements :244-248, drawTowers :297-301, updateAI :425-450 where opponentFaction is called) to use `resolveSide`/`opponentFaction` instead of the inline blocks and the private `opponentFaction` method. Delete the now-unused private `opponentFaction` method. Keep the gameState.role/playerFaction fallback defaults ('host'/'machines') at the call sites — resolveSide takes resolved non-null args. Behavior must be byte-identical: same faction mapping, same dir.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/sideHelper.ts` exists and exports `resolveSide` and `opponentFaction` (grep: `export function resolveSide` and `export function opponentFaction` both present)
    - sideHelper.ts imports nothing from phaser, supabase, or gameState (grep -v '^//' src/lib/sideHelper.ts | grep -cE "phaser|supabase|gameState" returns 0)
    - GameScene.ts no longer defines a private `opponentFaction` method (grep -c "private opponentFaction" src/scenes/GameScene.ts returns 0)
    - GameScene.ts imports resolveSide (grep: `from '../lib/sideHelper'` present)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>resolveSide/opponentFaction live in one pure module; all three GameScene duplication sites call it; tsc clean; faction mapping unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/towers/TowerData.ts (flat static stats, D-10)</name>
  <files>src/towers/TowerData.ts, src/scenes/GameScene.ts</files>
  <read_first>
    - src/units/UnitData.ts (the exact structural template — flat exported constants/table, no Phaser)
    - src/scenes/GameScene.ts (lines 31-38 TowerDef interface; lines 168-180 inline TOWER_RANGE/TOWER_DMG/TOWER_CD + the 6-tower build loop in create())
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (section "src/towers/TowerData.ts" — mirror shape, D-10 no per-level scaling)
  </read_first>
  <action>
    Create `src/towers/TowerData.ts` mirroring `UnitData.ts` style: import only `CELL` from `../maps/MapData`, no Phaser, no Supabase. Export flat constants `TOWER_RANGE = 6 * CELL` (216px), `TOWER_DMG = 25`, `TOWER_CD = 1400` (ms), exactly the current GameScene :168-170 values. Export a `TowerDefinition` interface `{ range: number; dmg: number; maxCd: number }` and a `TOWER_DEF: TowerDefinition = { range: TOWER_RANGE, dmg: TOWER_DMG, maxCd: TOWER_CD }`. Do NOT model per-level upgrade scaling (that is PROG-02/Phase 12). Then update GameScene's `create()` tower-build loop (:168-180) to import these constants from `../towers/TowerData` instead of declaring them locally. The `this.towers` array shape (the current inline `TowerDef` interface at :32-38 with cx/cy/slotIdx/isHostSide/range/dmg/cd/maxCd) stays in GameScene for now — only the magic-number source moves. Behavior identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/towers/TowerData.ts` exists and exports `TOWER_RANGE`, `TOWER_DMG`, `TOWER_CD` (grep all three `export const TOWER_` names present)
    - TowerData.ts imports nothing from phaser or supabase (grep -v '^//' src/towers/TowerData.ts | grep -cE "phaser|supabase" returns 0)
    - TOWER_RANGE = 6 * CELL, TOWER_DMG = 25, TOWER_CD = 1400 literally (grep confirms `25`, `1400`, `6 * CELL`)
    - GameScene.ts imports from '../towers/TowerData' (grep present) and no longer declares `const TOWER_DMG = 25` inline (grep -c "const TOWER_DMG" src/scenes/GameScene.ts returns 0)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Tower stat constants live in src/towers/TowerData.ts (flat, no scaling); GameScene imports them; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extract tower rendering into src/towers/TowerView.ts (D-09)</name>
  <files>src/towers/TowerView.ts, src/scenes/GameScene.ts</files>
  <read_first>
    - src/scenes/GameScene.ts (lines 293-317 drawTowers; lines 51-56 FC color table + fac() helper)
    - src/units/UnitView analog described in 10-PATTERNS.md (UnitView is the new Phaser-render-half pattern this mirrors)
    - .planning/phases/10-services-simulation-refactor/10-PATTERNS.md (section "src/towers/TowerView.ts" — exact draw source + FC table relocation)
    - src/lib/sideHelper.ts (created in Task 1 — drawTowers' side resolution now uses resolveSide)
  </read_first>
  <action>
    Create `src/towers/TowerView.ts` containing the tower draw logic extracted from `GameScene.drawTowers` (:293-317). Provide an exported function `drawTowers(scene: Phaser.Scene, towers: readonly { cx: number; cy: number; isHostSide: boolean }[], hostFaction: string, guestFaction: string): void` (or a small class wrapping a single `Phaser.GameObjects.Graphics` — Claude's choice) that creates a depth-3 graphics object and draws each tower's shadow/body/highlight/border/battlements exactly as the current code does (tw=28, th=28, the four fillStyle/lineStyle calls, the 3-battlement loop). Move the `FC` color table and `fac()` helper (:51-56) into TowerView.ts (or a small shared colors module if cleaner — Claude's discretion). Towers are static (no animation, no syncFrom). Update GameScene.create() to call the extracted `drawTowers(this, this.towers, hostFaction, guestFaction)` using `resolveSide(role, playerFaction)` (from Task 1) to compute the factions, and delete the private `drawTowers` method body from GameScene. The pixel output must be identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/towers/TowerView.ts` exists and exports a `drawTowers` function/class (grep: `export function drawTowers` or `export class` present)
    - GameScene.ts no longer defines `private drawTowers` (grep -c "private drawTowers" src/scenes/GameScene.ts returns 0)
    - The FC color table is no longer duplicated — defined in exactly one location (grep -rc "machines: { fill: 0x1a5090" src/ returns 1)
    - TowerView.ts uses resolveSide-derived factions or receives them as params (no inline gameState faction-mapping duplication reintroduced)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Tower rendering lives in src/towers/TowerView.ts mirroring the View split; GameScene calls it; FC table de-duplicated; tsc clean; towers draw identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase broadcast | Untrusted opponent-supplied deploy/wall/base_hp/game_over payloads (unchanged this phase) |

This plan introduces NO new external surface, NO new I/O, and touches NO networking or persistence code. It moves static constants and pure render code between local files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Existing Supabase broadcast trust boundary | accept | Out of scope — Phase 10 preserves the existing trust-and-overwrite wire behavior byte-for-byte (D-04); server-authority hardening is deferred to Phase 11/14. This plan does not touch networking. |
| T-10-01-NEW | — | new external surface | accept | None introduced. TowerData/TowerView/sideHelper are pure local modules with zero new I/O. Extraction must NOT weaken existing trust boundaries: the sim stays transport-free, the scene keeps owning networking, no client-authority added or removed. |

**Honest assessment:** This is a behavior-preserving internal refactor. The threat register is near-empty by design.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0 after each task (build stays green at every commit)
- `npx vitest run` — existing Phase 9 tests still green (no sim tests yet; this plan adds none)
- Manual: towers render in the correct faction colors at the correct positions; towers fire and damage units exactly as before (deferred to the phase-end manual parity playtest, D-16)
</verification>

<success_criteria>
- ROADMAP SC#4 satisfied: towers promoted out of inline GameScene into dedicated `src/towers/` module consistent with the Unit data/view split
- D-09 (data + view in src/towers/, logic deferred to sim), D-10 (flat static table, no scaling), D-11 (single resolveSide helper) all implemented
- Build tsc-green; no behavior change (parity gate is the phase-end manual playtest)
</success_criteria>

<output>
Create `.planning/phases/10-services-simulation-refactor/10-01-SUMMARY.md` when done.
</output>
